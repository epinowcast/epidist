# The theme the package plots use

[`ggplot2::theme_minimal()`](https://ggplot2.tidyverse.org/reference/ggtheme.html)
with the legend below the plot, as the package documentation draws its
plots.

## Usage

``` r
.epidist_plot_theme()
```

## Value

A `ggplot2` theme.
