# The single censoring window of a simulated study

The single censoring window of a simulated study

## Usage

``` r
.study_window(width, event)
```

## Arguments

- width:

  The window widths of every case.

- event:

  Which event the window belongs to, used in the error.

## Value

The window width.
