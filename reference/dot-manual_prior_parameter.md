# Extract the parameter name of a manually specified `brms` prior

Extract the parameter name of a manually specified `brms` prior

## Usage

``` r
.manual_prior_parameter(prior)
```

## Arguments

- prior:

  One or more manually specified prior distributions in the class
  `brmsprior`.

## Value

A character vector of parameter names.
