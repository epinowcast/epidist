# The studies of a meta model, in order of first appearance

The studies of a meta model, in order of first appearance

## Usage

``` r
.leave_one_out_studies(data)
```

## Arguments

- data:

  An `epidist_meta_model` object or the model data of a meta model fit,
  with a `study` column.

## Value

A character vector of study labels.
