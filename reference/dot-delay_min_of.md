# Find the minimum delay the datasets record

Find the minimum delay the datasets record

## Usage

``` r
.delay_min_of(datasets)
```

## Arguments

- datasets:

  A list of `epidist_linelist_data` objects.

## Value

The single minimum delay the datasets record, or `NULL` when they do not
all record the same one.
