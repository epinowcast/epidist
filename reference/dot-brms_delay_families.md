# The delay families whose `brms` post-processing `epidist` replaces

The delay families whose `brms` post-processing `epidist` replaces

## Usage

``` r
.brms_delay_families()
```

## Value

A character vector of `brms` family names.
