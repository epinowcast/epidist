# The element names of a multivariate representation

The element names of a multivariate representation

## Usage

``` r
.multivariate_names(params, index)
```

## Arguments

- params:

  The parameter names, in order.

- index:

  The trajectory points, in order.

## Value

A character vector of element names, ordered index major and parameter
minor.
