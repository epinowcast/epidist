# The parameterisations supported for a reported distribution

Each family lists the sets of parameter names a study may report it
with. The first set of each family is the one used internally.

## Usage

``` r
.estimates_parameter_sets()
```

## Value

A named list of character vectors of parameter names.
