# Whether any summary row of a meta model estimates its growth rate

Whether any summary row of a meta model estimates its growth rate

## Usage

``` r
.meta_growth_estimated(data)
```

## Arguments

- data:

  An `epidist_meta_model` object.

## Value

`TRUE` where a summary row has a `growth_known` slot of 0.
