# Extract the specific `epidist` classes of an object

Extract the specific `epidist` classes of an object

## Usage

``` r
.epidist_classes(data)
```

## Arguments

- data:

  An object.

## Value

A character vector of the `epidist` classes of `data`, excluding the
shared `epidist_data` class.
