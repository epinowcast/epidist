# The log of a sum of exponentials over each row of a matrix

The log of a sum of exponentials over each row of a matrix

## Usage

``` r
.meta_row_log_sum_exp(m)
```

## Arguments

- m:

  A numeric matrix of log values.

## Value

A numeric vector, one value per row, `-Inf` where every element of the
row is `-Inf`.
