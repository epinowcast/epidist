# What each element of a multivariate representation reports

What each element of a multivariate representation reports

## Usage

``` r
.estimates_multivariate_types(params)
```

## Arguments

- params:

  The parameter names of an `epidist_multivariate` object.

## Value

A list with the `type` and `p` of each element.
