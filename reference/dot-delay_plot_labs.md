# Label the axes of a delay plot and theme it

Label the axes of a delay plot and theme it

## Usage

``` r
.delay_plot_labs(p)
```

## Arguments

- p:

  A `ggplot` object.

## Value

A `ggplot` object.
