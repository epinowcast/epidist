# The smallest quantile standard error the model will use

A quantile standard error supplied on the delay scale is used as
reported, held at or above this value as a guard against a standard
error of zero, which would give a degenerate likelihood.

## Usage

``` r
.meta_min_prob_se()
```

## Value

A delay scale standard error.
