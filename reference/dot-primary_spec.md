# Look up a primary event distribution

Look up a primary event distribution

## Usage

``` r
.primary_spec(primary)
```

## Arguments

- primary:

  The distribution name.

## Value

The registry entry for `primary`.
