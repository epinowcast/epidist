# The reported covariance matrices of an `epidist_estimates_data` object

The reported covariance matrices of an `epidist_estimates_data` object

## Usage

``` r
.estimates_vcov(data)
```

## Arguments

- data:

  An `epidist_estimates_data` object.

## Value

A named list of covariance matrices, empty when no study reported one.
