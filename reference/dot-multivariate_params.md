# The parameter columns of a data frame of draws

The parameter columns of a data frame of draws

## Usage

``` r
.multivariate_params(params, draws, structural)
```

## Arguments

- params:

  The columns the user gave, or `NULL`.

- draws:

  A data frame of draws.

- structural:

  The index and draw columns.

## Value

A character vector of column names.
