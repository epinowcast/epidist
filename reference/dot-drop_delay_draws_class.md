# Drop the `epidist_delay_draws` class from an object

Drop the `epidist_delay_draws` class from an object

## Usage

``` r
.drop_delay_draws_class(data)
```

## Arguments

- data:

  An object to drop the class from.

## Value

`data` without the `epidist_delay_draws` class.
