# Which rows of an `epidist_estimates_data` object have a covariance matrix

Which rows of an `epidist_estimates_data` object have a covariance
matrix

## Usage

``` r
.estimates_vcov_rows(data)
```

## Arguments

- data:

  An `epidist_estimates_data` object.

## Value

A logical vector, one entry per row.
