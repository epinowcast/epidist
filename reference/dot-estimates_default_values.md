# Default values `as_epidist_estimates_data()` assumes for metadata columns

Shared by
[`.fill_estimates_defaults()`](https://epidist.epinowcast.org/reference/dot-fill_estimates_defaults.md)
and
[`.epireview_default()`](https://epidist.epinowcast.org/reference/dot-epireview_default.md)
so the two stay in step.

## Usage

``` r
.estimates_default_values
```

## Format

An object of class `list` of length 7.
