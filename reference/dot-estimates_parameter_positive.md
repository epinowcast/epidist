# Which reported parameters are constrained to be positive

Which reported parameters are constrained to be positive

## Usage

``` r
.estimates_parameter_positive(names)
```

## Arguments

- names:

  The names of the reported parameters.

## Value

A logical vector, one entry per parameter.
