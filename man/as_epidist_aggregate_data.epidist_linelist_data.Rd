% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_data.R
\name{as_epidist_aggregate_data.epidist_linelist_data}
\alias{as_epidist_aggregate_data.epidist_linelist_data}
\title{Convert linelist data to aggregate format}
\usage{
\method{as_epidist_aggregate_data}{epidist_linelist_data}(data, by = NULL, ...)
}
\arguments{
\item{data}{The data to convert}

\item{by}{Character vector of additional variables to stratify by, beyond the
required time variables.}

\item{...}{Additional arguments passed to methods}
}
\description{
This method takes an \code{epidist_linelist_data} object (see
\code{\link[=as_epidist_linelist_data]{as_epidist_linelist_data()}}) and aggregates it by counting unique
combinations of the required time variables (primary/secondary event times
and observation time) and any additional variables specified in \code{by}. The
result is a more compact representation of the same data where each row
represents multiple identical observations with the count stored in the \code{n}
column.
}
\examples{
# Default stratification by required time variables only
sierra_leone_ebola_data |>
  as_epidist_linelist_data(
    pdate_lwr = "date_of_symptom_onset",
    sdate_lwr = "date_of_sample_tested"
  ) |>
  as_epidist_aggregate_data()

# Additional stratification by other variables
sierra_leone_ebola_data |>
  as_epidist_linelist_data(
    pdate_lwr = "date_of_symptom_onset",
    sdate_lwr = "date_of_sample_tested"
  ) |>
  as_epidist_aggregate_data(by = "age")
}
\seealso{
Other aggregate_data: 
\code{\link{as_epidist_aggregate_data}()},
\code{\link{as_epidist_aggregate_data.data.frame}()},
\code{\link{as_epidist_aggregate_data.default}()},
\code{\link{assert_epidist.epidist_aggregate_data}()},
\code{\link{is_epidist_aggregate_data}()},
\code{\link{new_epidist_aggregate_data}()}
}
\concept{aggregate_data}
