% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/newdata.R
\name{.build_newdata}
\alias{.build_newdata}
\title{Expand variables into a grid and add the variables a model needs}
\usage{
.build_newdata(data, ..., .cols, .supplied = character())
}
\arguments{
\item{data}{An \code{epidist} data object.}

\item{...}{Variables to expand into a grid, passed to \code{\link[tidyr:expand]{tidyr::expand()}}.}

\item{.cols}{A named list of the variables to add, each of which is crossed
with the grid.}

\item{.supplied}{Names of the method's own arguments that the user gave,
usually \code{intersect(names(match.call()), names(formals()))}. A name that was
both expanded and supplied is an error. Names passed through \code{...} are not
included, so setting a column with the \code{tidyr::expand()} syntax, such as
\code{pwindow = 1:2}, still works.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of \code{newdata}.
}
\description{
Used by the \code{\link[=epidist_newdata]{epidist_newdata()}} methods. Any \code{epidist} class is dropped
before expanding so that the class methods documented in \link{epidist_data} do
not warn about an object that was never meant to stay in its class.
}
\keyword{internal}
