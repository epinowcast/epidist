% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{epidist_strata}
\alias{epidist_strata}
\title{Unique combinations of the predictors in a model}
\usage{
epidist_strata(object, vars = NULL)
}
\arguments{
\item{object}{A model fit with \code{\link[=epidist]{epidist()}}.}

\item{vars}{A character vector of variables to take unique combinations of.
If \code{NULL}, the default, the variables in the distributional parameter
formulas are used.}
}
\value{
A \code{tibble} with one row per unique combination of \code{vars}, with the
combination columns first.
}
\description{
Returns one row of the model data for each unique combination of the
variables used to predict the delay distribution parameters. Passing this to
\code{\link[=delay_parameter_draws]{delay_parameter_draws()}} gives one set of draws per combination rather than
one per observation, which is the same result with far fewer draws.
}
\details{
The variables are taken from the right hand side of each distributional
parameter formula. The remaining columns are kept from the first row of the
model data in which each combination occurs. This keeps the model variables
that \code{brms} requires in \code{newdata}, such as the relative observation time and
the censoring windows for the latent and marginal models. Those variables do
not enter the distributional parameters, so the values kept do not change
the draws.

A model with only an intercept has no predictors and so returns a single
row. A continuous predictor has as many combinations as it has distinct
values, so consider passing \code{vars} and a grid of your own instead.
}
\examples{
\dontrun{
# `fit` is a model fitted with `epidist()`
epidist_strata(fit)
}
}
\seealso{
Other postprocess:
\code{\link[=add_mean_sd]{add_mean_sd()}},
\code{\link[=add_mean_sd.default]{add_mean_sd.default()}},
\code{\link[=add_mean_sd.gamma_samples]{add_mean_sd.gamma_samples()}},
\code{\link[=add_mean_sd.lognormal_samples]{add_mean_sd.lognormal_samples()}},
\code{\link[=add_mean_sd.weibull_samples]{add_mean_sd.weibull_samples()}},
\code{\link[=add_summaries]{add_summaries()}},
\code{\link[=delay_parameter_draws]{delay_parameter_draws()}},
\code{\link[=predict_delay_parameters]{predict_delay_parameters()}}
}
\concept{postprocess}
