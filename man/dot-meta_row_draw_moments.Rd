% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_row_draw_moments}
\alias{.meta_row_draw_moments}
\title{The implied summaries of one meta model row for every posterior draw}
\usage{
.meta_row_draw_moments(slots, dist, dist_args)
}
\arguments{
\item{slots}{The output of \code{\link[=.meta_row_slots]{.meta_row_slots()}}.}

\item{dist}{A \code{primarycensored} distribution function name.}

\item{dist_args}{A list of named parameter lists, one per posterior draw.}
}
\value{
A list of summary vectors, one per posterior draw.
}
\description{
Rows sharing a study design, the same parameter draws and the same
quadrature resolution imply the same summaries, so they are computed once
and reused. The cache is bounded and lives in the package namespace, so it
is never written into a fitted model object. See \link{.meta_draws}.
}
\details{
Only reported means and standard deviations need implied summaries. Quantile
rows work on the cumulative probability scale, so they get a list of \code{NULL}
and nothing is computed for them.
}
\keyword{internal}
