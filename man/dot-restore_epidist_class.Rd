% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epidist_data.R
\name{.restore_epidist_class}
\alias{.restore_epidist_class}
\title{Put the \code{epidist} classes of a template back on an object}
\usage{
.restore_epidist_class(data, template)
}
\arguments{
\item{data}{A \code{data.frame} to add the classes to.}

\item{template}{The object whose \code{epidist} classes to add.}
}
\value{
\code{data} with the \code{epidist} classes of \code{template}, or \code{data}
unchanged if it is not a \code{data.frame} or \code{template} has none.
}
\description{
\code{dplyr} builds a new grouped tibble rather than keeping the class of its
input, so the \code{epidist} classes are lost whenever a grouped object is
modified. This puts them back, in front of the classes \code{data} already has,
so that a grouped result carries the \code{epidist} classes followed by
\code{grouped_df}. The result still needs checking, which
\code{\link[=.revalidate_epidist]{.revalidate_epidist()}} does after calling this.
}
\keyword{internal}
