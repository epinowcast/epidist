% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_deep_tail}
\alias{.meta_deep_tail}
\title{Whether a delay is certainly below the cut of the distribution function}
\usage{
.meta_deep_tail(q, dist, args)
}
\arguments{
\item{q}{A numeric vector of delays.}

\item{dist}{A \code{primarycensored} distribution function name.}

\item{args}{A named list of distribution parameters.}
}
\value{
A logical vector.
}
\description{
Decided from a closed form bound on the parameters rather than from the
distribution function, because in Stan the distribution function must not
be evaluated where it underflows: its autodiff partial is then \code{0 / 0},
and Stan's reverse pass chains every node on the stack, so the \code{NaN}
poisons the gradient even when the value is discarded. The bounds are
\code{Phi(z) < exp(-100)} for \code{z < -14} for the lognormal,
\code{P(a, x) <= x^a / Gamma(a + 1)} for the gamma and \code{1 - exp(-y) <= y} for
the weibull. Mirrors \code{meta_family_deep_tail()} in Stan.
}
\keyword{internal}
