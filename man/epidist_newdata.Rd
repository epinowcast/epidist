% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/newdata.R
\name{epidist_newdata}
\alias{epidist_newdata}
\title{Build \code{newdata} for prediction from an \code{epidist} data object}
\usage{
epidist_newdata(data, ...)
}
\arguments{
\item{data}{An \code{epidist} data object, such as one returned by
\code{\link[=as_epidist_marginal_model]{as_epidist_marginal_model()}}, \code{\link[=as_epidist_latent_model]{as_epidist_latent_model()}} or
\code{\link[=as_epidist_naive_model]{as_epidist_naive_model()}}.}

\item{...}{Variables to expand into a grid, passed to \code{\link[tidyr:expand]{tidyr::expand()}}.
Supply the variables used in the model formula, such as \code{sex}. Each
combination of their unique values becomes a row. Supply no variables to
get a single row, which is what an intercept only model needs. A variable
expanded here keeps its expanded values, so naming it as an argument of
the method as well is an error.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of \code{newdata} ready to predict from.
}
\description{
Prediction with \code{brms} and \code{tidybayes} needs a \code{newdata} argument holding
every variable the model uses. As well as the variables in your formula
that means the response and the observation process variables that
\code{epidist} adds. Their names depend on the model, so building \code{newdata} by
hand means knowing how each model is specified. This function builds it for
you. It expands the variables you supply into a grid, in the same way as
\code{\link[tidyr:expand]{tidyr::expand()}}, and adds the rest with values you set through named
arguments.
}
\details{
The defaults give the delay distribution with no censoring and no
truncation. Set \code{pwindow} and \code{swindow} to ask for censoring, and
\code{relative_obs_time} to ask for truncation. See the method for each model
for the arguments it takes.

The result is a plain \code{\link[tibble:tibble]{tibble::tibble()}}. Pass it to
\code{\link[brms:posterior_epred.brmsfit]{brms::posterior_epred()}}, \code{\link[brms:posterior_predict.brmsfit]{brms::posterior_predict()}},
\code{\link[=add_delay_parameter_draws]{add_delay_parameter_draws()}} or \code{\link[=delay_parameter_draws]{delay_parameter_draws()}}, or to the
\code{tidybayes} functions \code{add_epred_draws()} and \code{add_predicted_draws()}.
}
\examples{
prep_obs <- sierra_leone_ebola_data |>
  as_epidist_linelist_data(
    pdate_lwr = "date_of_symptom_onset",
    sdate_lwr = "date_of_sample_tested"
  ) |>
  as_epidist_marginal_model()

# An intercept only model
epidist_newdata(prep_obs)

# A row for each sex
epidist_newdata(prep_obs, sex)
}
\seealso{
Other newdata: 
\code{\link{epidist_newdata.default}()},
\code{\link{epidist_newdata.epidist_latent_model}()},
\code{\link{epidist_newdata.epidist_marginal_model}()},
\code{\link{epidist_newdata.epidist_naive_model}()}
}
\concept{newdata}
