% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate.R
\name{new_epidist_multivariate}
\alias{new_epidist_multivariate}
\title{Class constructor for \code{epidist_multivariate} objects}
\usage{
new_epidist_multivariate(
  value,
  vcov,
  params,
  index = 1,
  n_draws = NA_integer_,
  draws = NULL
)
}
\arguments{
\item{value}{A named numeric vector of the mean of each element.}

\item{vcov}{The covariance matrix of \code{value}.}

\item{params}{A character vector of the parameter names, in order.}

\item{index}{The trajectory points, in order. Defaults to a single point.}

\item{n_draws}{The number of draws \code{value} and \code{vcov} were computed from, or
\code{NA} where they were published directly.}

\item{draws}{A matrix of draws with one row per draw and one column per
element of \code{value}, or \code{NULL}.}
}
\value{
An object of class \code{epidist_multivariate}.
}
\description{
Use this where a study published a mean vector and a covariance matrix
directly, rather than draws they can be computed from. Converting the object
with a \code{family}, which needs the draws themselves, is then unavailable.
}
\examples{
new_epidist_multivariate(
  value = c(mean = 7.5, sd = 3.6),
  vcov = matrix(c(0.09, 0.02, 0.02, 0.04), nrow = 2),
  params = c("mean", "sd")
)
}
\seealso{
Other multivariate:
\code{\link[=as_epidist_multivariate]{as_epidist_multivariate()}},
\code{\link[=as_epidist_multivariate.data.frame]{as_epidist_multivariate.data.frame()}},
\code{\link[=as_epidist_multivariate.matrix]{as_epidist_multivariate.matrix()}},
\code{\link[=assert_epidist.epidist_multivariate]{assert_epidist.epidist_multivariate()}},
\code{\link[=is_epidist_multivariate]{is_epidist_multivariate()}},
\code{\link[=print.epidist_multivariate]{print.epidist_multivariate()}},
\code{\link[=vcov.epidist_multivariate]{vcov.epidist_multivariate()}}
}
\concept{multivariate}
