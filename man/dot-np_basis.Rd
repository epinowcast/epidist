% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonparametric.R
\name{.np_basis}
\alias{.np_basis}
\title{Build the basis of the hazard formula over the bins}
\usage{
.np_basis(formula, boundaries)
}
\arguments{
\item{formula}{A one sided formula for the logit hazards over the bins,
see Details. The default, \code{NULL}, is a spline over the delay.}

\item{boundaries}{A numeric vector of at least four strictly increasing
bin boundaries, \eqn{b_0} to \eqn{b_K}. The default, \code{NULL}, is set by
\code{\link[=epidist]{epidist()}} from the data, with a bin for every whole delay from 0 up to
the longest delay in the data, and at least four bins, that is
\code{seq(-1, max(3, max_delay))}. The last boundary must be at least as long
as the longest observed delay.}
}
\value{
A list holding \code{basis}, a matrix with one row per free bin and
one column per coefficient, and \code{coefficients}, a \code{data.frame} with one
row per column giving the \code{term} of the column, its parameter \code{dpar},
and the standard deviation parameter \code{sd} of its penalised term, \code{NA}
for an unpenalised column.
}
\description{
Parametric terms come from \code{\link[stats:model.matrix]{stats::model.matrix()}} and smooths from
\code{\link[mgcv:smoothCon]{mgcv::smoothCon()}} and \code{\link[mgcv:smooth2random]{mgcv::smooth2random()}}, which split each smooth
into unpenalised columns and penalised blocks with one standard deviation
each, as \code{brms} does. Each column is centred over the bins, each
unpenalised column is scaled to unit standard deviation and each
penalised block to unit root mean square.
}
\keyword{internal}
