% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_joint_covariance}
\alias{.meta_joint_covariance}
\title{The sampling covariance of the summaries one study reports}
\usage{
.meta_joint_covariance(types, probs, moments, density, partial, study_n)
}
\arguments{
\item{types}{Member types, 1 for a mean, 2 for a standard deviation and 3
for a quantile.}

\item{probs}{Member probabilities, zero for a mean or standard deviation.}

\item{moments}{A summary vector from \code{\link[=.meta_moment_vector]{.meta_moment_vector()}}.}

\item{density}{The implied density at each implied quantile, one per
quantile member in order.}

\item{partial}{A two row matrix of centred partial moments from
\code{\link[=.meta_quantile_partials]{.meta_quantile_partials()}}, one column per quantile member in order.}

\item{study_n}{The number of delays the summaries were computed from.}
}
\value{
A covariance matrix over the members.
}
\description{
The asymptotic covariance of a sample mean, a sample standard deviation
and sample quantiles computed from the same \code{study_n} delays. The mean and
standard deviation block is that of \code{\link[=.meta_moment_pair_ll]{.meta_moment_pair_ll()}}. By the
Bahadur representation a sample quantile at probability \eqn{p} is
\eqn{Q_p - (\hat{G}(Q_p) - p) / f(Q_p)} up to a smaller order term, so
with \eqn{f_i = f(Q_{p_i})} the implied density at each implied quantile
\deqn{\text{Cov}(q_i, q_j) = \frac{p_i (1 - p_j)}{n f_i f_j}, \quad
p_i \le p_j,}
\deqn{\text{Cov}(\bar{x}, q_i) = -\frac{1}{n f_i}
\int_L^{Q_{p_i}} (x - \mu) \text{d}G(x),}
\deqn{\text{Cov}(s, q_i) = -\frac{1}{2 \sigma n f_i}
\int_L^{Q_{p_i}} \left((x - \mu)^2 - \sigma^2\right) \text{d}G(x),}
the last carried from the sample variance to the standard deviation by
the delta method. The integrals are the centred partial moments of
\code{\link[=.meta_quantile_partials]{.meta_quantile_partials()}}. Matches \code{meta_family_joint_covariance()} in
Stan.
}
\keyword{internal}
