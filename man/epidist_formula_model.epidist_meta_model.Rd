% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_model.R
\name{epidist_formula_model.epidist_meta_model}
\alias{epidist_formula_model.epidist_meta_model}
\title{Define the model-specific component of an \code{epidist} custom formula for the
meta model}
\usage{
\method{epidist_formula_model}{epidist_meta_model}(data, formula, ...)
}
\arguments{
\item{data}{An object with class corresponding to an implemented model.}

\item{formula}{An object of class \link[stats:formula]{stats::formula} or \link[brms:brmsformula]{brms::brmsformula}
(or one that can be coerced to those classes). A symbolic description of
the model to be fitted. A formula must be provided for the distributional
parameter \code{mu}, and may optionally be provided for other distributional
parameters.}

\item{...}{Additional arguments passed to method.}
}
\value{
A \code{brmsformula} object.
}
\description{
Adds the response and the slots of the meta model to the formula. Where a
summary row estimates its growth rate as the \code{pgrowth} distributional
parameter, see \code{\link[=as_epidist_meta_model]{as_epidist_meta_model()}}, and the formula has no term for
\code{pgrowth}, \code{pgrowth ~ 0 + study} is added so that each study has its own
rate and the priors \code{\link[=epidist_model_prior]{epidist_model_prior()}} builds from the reported
rates have a coefficient to act on. Data holding a single study get
\code{pgrowth ~ 1} instead, because \code{brms} cannot code a factor with one
level. A \code{pgrowth} formula given by the user is kept as it is.
}
\seealso{
Other meta_model: 
\code{\link{as_epidist_meta_model}()},
\code{\link{as_epidist_meta_model.NULL}()},
\code{\link{as_epidist_meta_model.epidist_aggregate_data}()},
\code{\link{as_epidist_meta_model.epidist_estimates_data}()},
\code{\link{as_epidist_meta_model.epidist_linelist_data}()},
\code{\link{assert_epidist.epidist_meta_model}()},
\code{\link{epidist_family_model.epidist_meta_model}()},
\code{\link{epidist_leave_one_out}()},
\code{\link{epidist_model_prior.epidist_meta_model}()},
\code{\link{epidist_newdata.epidist_meta_model}()},
\code{\link{epidist_transform_data_model.epidist_meta_model}()},
\code{\link{is_epidist_meta_model}()},
\code{\link{new_epidist_meta_model}()}
}
\concept{meta_model}
