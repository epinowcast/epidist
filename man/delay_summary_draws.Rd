% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{delay_summary_draws}
\alias{delay_summary_draws}
\title{Posterior draws of the delay distribution, summarised}
\usage{
delay_summary_draws(
  object,
  newdata = NULL,
  vars = NULL,
  probs = NULL,
  method = c("auto", "analytic", "sample"),
  nsim = 1000,
  ...
)
}
\arguments{
\item{object}{A model fit with \code{\link[=epidist]{epidist()}}.}

\item{newdata}{A \code{data.frame} of data to predict for. If \code{NULL}, the
default, \code{\link[=epidist_strata]{epidist_strata()}} builds one row per unique combination of the
predictors the model uses.}

\item{vars}{A character vector of variables to stratify by, passed to
\code{\link[=epidist_strata]{epidist_strata()}}. Only used when \code{newdata} is \code{NULL}.}

\item{probs}{A numeric vector of probabilities to add quantiles of the delay
distribution for. If \code{NULL}, the default, no quantiles are added.}

\item{method}{Passed to \code{\link[=add_summaries]{add_summaries()}}. Either \code{"auto"}, the default,
which uses the analytic solution when there is one and simulates
otherwise, \code{"analytic"}, or \code{"sample"}.}

\item{nsim}{The number of delays to simulate per row. Passed to
\code{\link[=add_summaries]{add_summaries()}} and only used when simulating.}

\item{...}{Additional arguments passed to \code{\link[brms:prepare_predictions]{brms::prepare_predictions()}},
such as \code{ndraws} or \code{re_formula}.}
}
\value{
A \code{tibble} of posterior draws of the delay distribution parameters
with \code{mean} and \code{sd} columns added, and one column per element of \code{probs}.
Grouped as \code{\link[=delay_parameter_draws]{delay_parameter_draws()}} returns it.
}
\description{
The usual last step of a fit. Builds one row per unique combination of the
predictors with \code{\link[=epidist_strata]{epidist_strata()}}, draws the delay distribution parameters
for each with \code{\link[=delay_parameter_draws]{delay_parameter_draws()}}, and adds the natural scale mean and
standard deviation, and any quantiles asked for, with \code{\link[=add_summaries]{add_summaries()}}.

Each of those three steps is available on its own where a step needs
different arguments, or where \code{newdata} is built some other way.
}
\examples{
\donttest{
fit <- sierra_leone_ebola_data |>
  as_epidist_linelist_data(
    pdate_lwr = "date_of_symptom_onset",
    sdate_lwr = "date_of_sample_tested"
  ) |>
  as_epidist_aggregate_data() |>
  as_epidist_marginal_model() |>
  epidist(chains = 2, cores = 2, refresh = ifelse(interactive(), 250, 0))

delay_summary_draws(fit, probs = c(0.05, 0.95))
}
}
\seealso{
\code{\link[=delay_parameter_draws]{delay_parameter_draws()}} for the parameters alone,
\code{\link[=add_summaries]{add_summaries()}} to summarise draws you already have, and
\code{\link[=epidist_strata]{epidist_strata()}} to build \code{newdata}.

Other postprocess: 
\code{\link{add_summaries}()},
\code{\link{delay_parameter_draws}()},
\code{\link{epidist_strata}()}
}
\concept{postprocess}
