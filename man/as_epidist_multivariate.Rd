% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate.R
\name{as_epidist_multivariate}
\alias{as_epidist_multivariate}
\title{Create an \code{epidist_multivariate} object}
\usage{
as_epidist_multivariate(draws, ...)
}
\arguments{
\item{draws}{Draws of the parameters. See the methods for supported formats.}

\item{...}{Additional arguments passed to methods.}
}
\description{
Summarises draws of a set of parameters by their mean vector and covariance
matrix. The result is a multivariate normal approximation to whatever the
draws describe, and it is the only route by which a covariance between
reported quantities reaches \code{\link[=as_epidist_meta_model]{as_epidist_meta_model()}}.
}
\details{
The draws may be of any parameters. Posterior draws of a delay mean and
standard deviation, of a \code{meanlog} and \code{sdlog}, or of a \code{shape} and a
\code{scale} all give the same object. Nothing here checks that a parameter is
something a study could report, because that only matters when the object is
converted with \code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}}.

Draws may also cover a trajectory, with the parameters varying over an
\code{index}. The elements are then ordered index major and parameter minor, so a
\code{mean} and an \code{sd} over two index points give \code{mean[1]}, \code{sd[1]}, \code{mean[2]},
\code{sd[2]}. The order is fixed because the covariance is indexed by it.

There is no method for a vector of individual delays. Resampling delays
manufactures a covariance the study never estimated, which is not what this
represents.
}
\seealso{
Other multivariate:
\code{\link[=as_epidist_multivariate.data.frame]{as_epidist_multivariate.data.frame()}},
\code{\link[=as_epidist_multivariate.matrix]{as_epidist_multivariate.matrix()}},
\code{\link[=assert_epidist.epidist_multivariate]{assert_epidist.epidist_multivariate()}},
\code{\link[=is_epidist_multivariate]{is_epidist_multivariate()}},
\code{\link[=new_epidist_multivariate]{new_epidist_multivariate()}},
\code{\link[=print.epidist_multivariate]{print.epidist_multivariate()}},
\code{\link[=vcov.epidist_multivariate]{vcov.epidist_multivariate()}}
}
\concept{multivariate}
