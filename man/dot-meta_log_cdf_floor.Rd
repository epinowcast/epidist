% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_log_cdf_floor}
\alias{.meta_log_cdf_floor}
\title{The log distribution function below which a node is severed}
\usage{
.meta_log_cdf_floor()
}
\value{
A log probability.
}
\description{
Every grid and quadrature path evaluates the distribution function from
the study's minimum delay upwards, so for a narrow delay it reaches deep
into the lower tail, where the Stan primary censored distribution function
of \code{primarycensored} returns a finite value with a non finite gradient.
A node whose plain log distribution function is below this value holds a
probability below \code{exp(-100)}, which no moment or probability the model
forms can resolve, so it is treated as holding no mass before that
function is called. Matches the cut in \code{meta_family_pcens_lcdf()} and
\code{meta_family_dist_prob()} in \code{inst/stan/meta_model/functions.stan}.
}
\keyword{internal}
