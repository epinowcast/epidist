% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior.R
\name{epidist_family_prior.gengamma}
\alias{epidist_family_prior.gengamma}
\title{Family specific prior distributions for the generalised gamma family}
\usage{
\method{epidist_family_prior}{gengamma}(family, formula, ...)
}
\arguments{
\item{family}{A description of the response distribution and link function to
be used in the model. Every family function has a link argument allowing
users to specify the link function to be applied on the response variable.
If not specified, default links are used. For details of all supported
families see \code{\link[brms:brmsfamily]{brms::brmsfamily()}}. Commonly used, such as
\code{\link[brms:brmsfamily]{brms::lognormal()}}, are also
reexported as part of \code{epidist}. The generalised gamma family
\code{\link[=gengamma]{gengamma()}} is defined by \code{epidist} itself.}

\item{formula}{An object of class \link[stats:formula]{stats::formula} or \link[brms:brmsformula]{brms::brmsformula}
(or one that can be coerced to those classes). A symbolic description of
the model to be fitted. A formula must be provided for the distributional
parameter \code{mu}, and may optionally be provided for other distributional
parameters.}

\item{...}{Additional arguments passed to \code{fn} method.}
}
\value{
A \code{brmsprior} object, or \code{NULL} when the model adds no priors.
}
\description{
The intercept of \code{mu}, the scale on the log scale, gets the \code{normal(1, 1)}
prior of the lognormal family. The intercepts of \code{shape} and \code{k}, both on
the log scale, get \code{normal(0, 0.5)} priors, which centre the family on
the gamma (\code{shape = 1}) and Weibull (\code{k = 1}) special cases with a 95\%
range of roughly 0.4 to 2.7 for each. The two shape parameters are only
weakly identified by a modest number of delays, so a wide prior on either
lets the sampler wander far into the tails.
}
\seealso{
Other prior: 
\code{\link{epidist_family_prior}()},
\code{\link{epidist_family_prior.default}()},
\code{\link{epidist_family_prior.lognormal}()},
\code{\link{epidist_model_prior}()},
\code{\link{epidist_model_prior.default}()},
\code{\link{epidist_model_prior.epidist_meta_model}()},
\code{\link{epidist_prior}()}
}
\concept{prior}
