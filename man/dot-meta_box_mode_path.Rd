% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_box_mode_path}
\alias{.meta_box_mode_path}
\title{The most likely path of the constrained chain of cumulative counts}
\usage{
.meta_box_mode_path(study_n, cdf, lower, upper, stride)
}
\arguments{
\item{study_n}{The number of delays the quantiles were computed from.}

\item{cdf}{The grid distribution function at the edges.}

\item{lower, upper}{The boxes at the edges.}

\item{stride}{The stride of the grid from \code{\link[=.meta_box_stride]{.meta_box_stride()}}.}
}
\value{
An integer vector of counts, one per edge, or \code{NULL} when no
path satisfies every box.
}
\description{
A Viterbi pass over the coarse grid of \code{\link[=.meta_box_grid]{.meta_box_grid()}} at every edge,
which finds the counts a study most probably had at each edge given
every box, to within a stride. The forward pass of \code{\link[=.meta_grid_box_ll]{.meta_grid_box_ll()}}
is then kept to a band around them. Centring on the counts the boxes
pull the chain to, rather than on the counts the parameters expect at
each edge, is what keeps the pass accurate where a later quantile forces
an earlier count far from its mean. The distribution function is rounded
to \eqn{2^{-20}} by \code{\link[=.meta_fixed_point]{.meta_fixed_point()}} first, as Stan must to keep the
search off the autodiff stack. Matches \code{meta_family_box_mode_path()} in
Stan.
}
\keyword{internal}
