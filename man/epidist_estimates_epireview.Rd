% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_epireview.R
\name{epidist_estimates_epireview}
\alias{epidist_estimates_epireview}
\title{Report studies from an \code{epireview} parameter table}
\usage{
epidist_estimates_epireview(
  data,
  study = "article_label",
  metadata = NULL,
  keep = NULL,
  advise = TRUE,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} of \code{epireview} delay estimates, such as the
\code{params} element of \code{epireview::load_epidata()} filtered to one delay.
It needs the \code{parameter_value}, \code{parameter_value_type} and
\code{population_sample_size} columns and the column named by \code{study}.
The other \verb{parameter_*} and \verb{distribution_*} columns are used where
present.}

\item{study}{A string naming the column of \code{data} that identifies the
study.
Defaults to \code{"article_label"}.}

\item{metadata}{A \code{data.frame} of study metadata, or \code{NULL}.
It needs a \code{study} column holding values of the \code{study} column of \code{data},
and takes any of \code{n}, \code{pwindow}, \code{swindow}, \code{relative_obs_time},
\code{trunc_adjusted}, \code{trunc_design}, \code{cens_adjusted}, \code{delay_min} and
\code{growth_rate}, as documented in \code{\link[=as_epidist_estimates_data.data.frame]{as_epidist_estimates_data.data.frame()}}.
Each value replaces the value of that column for the study's records, and
an \code{NA} leaves it as it is.}

\item{keep}{A character vector of columns of \code{data} to carry onto the rows
of the result, for use as covariates.
Defaults to \code{NULL}.}

\item{advise}{Whether to run the advisory checks of
\code{\link[=as_epidist_estimates_data.data.frame]{as_epidist_estimates_data.data.frame()}}.
Defaults to \code{TRUE}.}

\item{...}{Study metadata applied to every record, each a single value, as
documented in \code{\link[=as_epidist_estimates_data.data.frame]{as_epidist_estimates_data.data.frame()}}.
Any of \code{pwindow}, \code{swindow}, \code{relative_obs_time}, \code{trunc_adjusted},
\code{trunc_design}, \code{cens_adjusted}, \code{delay_min} and \code{growth_rate}.}
}
\value{
An \code{epidist_estimates_data} object.
}
\description{
Maps a table of published delay estimates from the
\href{https://mrc-ide.github.io/epireview/}{epireview} package, one row per
reported estimate, to the long format \code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}} takes,
and returns the \code{epidist_estimates_data} object it builds.
\code{epireview} collates the parameter estimates gathered by the Pathogen
Epidemiology Review Group.
It is not on CRAN, and is installed from the
\href{https://mrc-ide.r-universe.dev}{mrc-ide r-universe}.
Filter the \code{params} table of \code{epireview::load_epidata()} to one delay
before passing it here, because the rows of one object all describe the
same delay.
}
\section{What each record becomes}{
A record is mapped by its \code{parameter_value_type}.
\itemize{
\item A \code{"Mean"} becomes a \code{"mean"} row.
A \code{"Standard Deviation"} in \code{parameter_uncertainty_singe_type} becomes a
matching \code{"sd"} row, as does the second parameter of a fitted
distribution that \code{epireview} records as a mean and a standard deviation.
A \code{"Standard Error"} becomes the \code{se} of the mean row instead, and so does
a 95\% confidence or credible interval of the mean, as its width over
\code{2 * qnorm(0.975)}.
\item A \code{"Median"} becomes a \code{"quantile"} row at \code{p = 0.5}, with further rows at
\code{p = 0.25} and \code{p = 0.75} where \code{parameter_uncertainty_type} is \code{"IQR"}.
\item A record whose \code{distribution_type} is a gamma, a Weibull or a lognormal
reported by its natural parameters (a shape with a scale or a rate, or a
meanlog with a sdlog) becomes the mean and standard deviation the fitted
distribution implies over the delays the study could have seen, as
\code{\link[=epidist_estimates_parameters]{epidist_estimates_parameters()}} computes them.
Its reported value and spread are not used again, because they are
functions of the same parameters.
\code{epireview} records no uncertainty for the parameters, so the rows take
their sampling uncertainty from the sample size.
}

A range is the smallest and largest delay a study saw rather than a summary
of the distribution, and is not used.
A spread that does not match the value type, such as a standard deviation
reported alongside a median, is not used either, because the two kinds of
summary from a study that reported integer date differences are fitted as
though they were independent.

Records that cannot be mapped are dropped with a message naming them.
These are records reporting an inverse rate, units other than days, a value
with a scaling exponent, no mean or median and no usable distribution
parameters, and records left with no sample size and no standard error.
Fill a missing sample size through \code{metadata}.

Several records may share a study, for example estimates stratified by
outbreak or by group, and they are then fitted as separate summaries of the
same study.
}

\section{Study metadata}{
\code{epireview} does not record how a study handled censoring or right
truncation, its censoring windows or its observation time.
Give the metadata that applies to every record through \code{...}, and the
metadata that differs by study through \code{metadata}, a data frame with one
row per study whose values replace those of \code{...} for that study's records.
Anything given for neither is assumed as \code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}}
assumes it, with the same messages, and a study that \code{metadata} leaves
blank for a column it holds is assumed in the same way and named.
The Checks section of \code{\link[=as_epidist_estimates_data.data.frame]{as_epidist_estimates_data.data.frame()}} says which
assumptions matter most.

The result is an \code{epidist_estimates_data} object, so metadata learnt later
can be edited in with \code{\link[dplyr:mutate]{dplyr::mutate()}} or \verb{[<-}.
The object is checked again after each change and drops its class if the
change breaks a requirement, so make related changes, such as setting
\code{trunc_adjusted} to \code{FALSE} and giving the \code{relative_obs_time} it then
needs, in one call.
See \link{epidist_data}.
}

\examples{
# A table with the columns epireview uses
records <- data.frame(
  article_label = c("A 2015", "B 2016", "B 2016"),
  parameter_value = c(10.6, 14, 8.9),
  parameter_value_type = c("Mean", "Median", "Mean"),
  parameter_unit = "Days",
  parameter_uncertainty_single_value = c(3.2, NA, NA),
  parameter_uncertainty_singe_type = c("Standard Deviation", NA, NA),
  parameter_uncertainty_lower_value = c(NA, 11, NA),
  parameter_uncertainty_upper_value = c(NA, 15, NA),
  parameter_uncertainty_type = c(NA, "IQR", NA),
  population_sample_size = c(76, 20, 92)
)
epidist_estimates_epireview(
  records,
  metadata = data.frame(
    study = "A 2015", relative_obs_time = 60, trunc_adjusted = FALSE
  ),
  trunc_adjusted = TRUE,
  cens_adjusted = 0
)
\dontshow{if (requireNamespace("epireview", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
ebola <- suppressMessages(epireview::load_epidata("ebola"))$params
onset_to_death <- ebola[
  ebola$parameter_type_short == "delay_onset_to_death",
]
epidist_estimates_epireview(
  onset_to_death,
  trunc_adjusted = TRUE,
  cens_adjusted = 0,
  keep = "method_moment_value"
)
\dontshow{\}) # examplesIf}
}
\seealso{
Other estimates_data: 
\code{\link{as_epidist_estimates_data}()},
\code{\link{as_epidist_estimates_data.data.frame}()},
\code{\link{as_epidist_estimates_data.epidist_estimates_data}()},
\code{\link{as_epidist_estimates_data.epidist_multivariate}()},
\code{\link{as_epidist_estimates_data.list}()},
\code{\link{assert_epidist.epidist_estimates_data}()},
\code{\link{epidist_estimates_parameters}()},
\code{\link{epidist_estimates_summaries}()},
\code{\link{is_epidist_estimates_data}()},
\code{\link{new_epidist_estimates_data}()}
}
\concept{estimates_data}
