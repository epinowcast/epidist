% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/naive_model.R
\name{epidist_newdata.epidist_naive_model}
\alias{epidist_newdata.epidist_naive_model}
\title{Build \code{newdata} for the naive model}
\usage{
\method{epidist_newdata}{epidist_naive_model}(data, ...)
}
\arguments{
\item{data}{An \code{epidist} data object, such as one returned by
\code{\link[=as_epidist_marginal_model]{as_epidist_marginal_model()}}, \code{\link[=as_epidist_latent_model]{as_epidist_latent_model()}} or
\code{\link[=as_epidist_naive_model]{as_epidist_naive_model()}}.}

\item{...}{Variables to expand into a grid, passed to \code{\link[tidyr:expand]{tidyr::expand()}}.
Supply the variables used in the model formula, such as \code{sex}. Each
combination of their unique values becomes a row. Supply no variables to
get a single row, which is what an intercept only model needs. A variable
expanded here keeps its expanded values, so naming it as an argument of
the method as well is an error.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of \code{newdata} ready to predict from.
}
\description{
The naive model accounts for neither censoring nor truncation, so the only
variable it uses beyond those in the model formula is the response. This
method adds it, set to \code{NA} because it is the quantity being predicted.
}
\examples{
prep_obs <- sierra_leone_ebola_data |>
  as_epidist_linelist_data(
    pdate_lwr = "date_of_symptom_onset",
    sdate_lwr = "date_of_sample_tested"
  ) |>
  as_epidist_naive_model()

# A row for each sex
epidist_newdata(prep_obs, sex)
}
\seealso{
Other naive_model:
\code{\link[=as_epidist_naive_model]{as_epidist_naive_model()}},
\code{\link[=as_epidist_naive_model.epidist_aggregate_data]{as_epidist_naive_model.epidist_aggregate_data()}},
\code{\link[=as_epidist_naive_model.epidist_linelist_data]{as_epidist_naive_model.epidist_linelist_data()}},
\code{\link[=epidist_formula_model.epidist_naive_model]{epidist_formula_model.epidist_naive_model()}},
\code{\link[=epidist_transform_data_model.epidist_naive_model]{epidist_transform_data_model.epidist_naive_model()}},
\code{\link[=is_epidist_naive_model]{is_epidist_naive_model()}},
\code{\link[=new_epidist_naive_model]{new_epidist_naive_model()}}

Other newdata:
\code{\link[=epidist_newdata]{epidist_newdata()}},
\code{\link[=epidist_newdata.default]{epidist_newdata.default()}},
\code{\link[=epidist_newdata.epidist_latent_model]{epidist_newdata.epidist_latent_model()}},
\code{\link[=epidist_newdata.epidist_marginal_model]{epidist_newdata.epidist_marginal_model()}}
}
\concept{naive_model}
\concept{newdata}
