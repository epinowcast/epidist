% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_shapes.R
\name{epidist_estimates_summaries}
\alias{epidist_estimates_summaries}
\title{Report a study that published summaries of its delays}
\usage{
epidist_estimates_summaries(
  study,
  mean = NULL,
  sd = NULL,
  quantiles = NULL,
  probs = NULL,
  se = NULL,
  n = NULL,
  ...
)
}
\arguments{
\item{study}{A string naming the study.}

\item{mean}{The reported mean delay. Optional.}

\item{sd}{The reported standard deviation of the delays. Optional.}

\item{quantiles}{A numeric vector of reported quantiles. Optional.}

\item{probs}{The probabilities of \code{quantiles}, in the same order. Required
where \code{quantiles} is given.}

\item{se}{A numeric vector of the reported standard errors of the
summaries, ordered mean, standard deviation, then quantiles, skipping any
that was not reported. Optional.}

\item{n}{The number of delays the study summarised. Optional.}

\item{...}{Study metadata, as documented in
\code{\link[=as_epidist_estimates_data.data.frame]{as_epidist_estimates_data.data.frame()}}.}
}
\value{
An \code{epidist_estimates_data} object.
}
\description{
Builds the rows \code{\link[=as_epidist_meta_model]{as_epidist_meta_model()}} takes from the summaries a study
published in wide form, one argument per kind of summary.
}
\details{
Give the uncertainty of each summary through \code{se}, or the number of delays
the study summarised through \code{n}, which the model uses to derive a sampling
uncertainty instead. One of the two is needed for every row.
}
\examples{
epidist_estimates_summaries(
  "study A",
  mean = 7.5, sd = 3.6, n = 120,
  relative_obs_time = 20, trunc_adjusted = FALSE, cens_adjusted = 0
)
}
\seealso{
Other estimates_data:
\code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}},
\code{\link[=as_epidist_estimates_data.data.frame]{as_epidist_estimates_data.data.frame()}},
\code{\link[=as_epidist_estimates_data.epidist_estimates_data]{as_epidist_estimates_data.epidist_estimates_data()}},
\code{\link[=as_epidist_estimates_data.epidist_multivariate]{as_epidist_estimates_data.epidist_multivariate()}},
\code{\link[=as_epidist_estimates_data.list]{as_epidist_estimates_data.list()}},
\code{\link[=assert_epidist.epidist_estimates_data]{assert_epidist.epidist_estimates_data()}},
\code{\link[=epidist_estimates_parameters]{epidist_estimates_parameters()}},
\code{\link[=is_epidist_estimates_data]{is_epidist_estimates_data()}},
\code{\link[=new_epidist_estimates_data]{new_epidist_estimates_data()}}
}
\concept{estimates_data}
