% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_reporting.R
\name{draws_to_multivariate}
\alias{draws_to_multivariate}
\title{Report draws of summary quantities as a published estimate}
\usage{
draws_to_multivariate(draws, study, type = NULL, p = NULL, value = NULL, ...)
}
\arguments{
\item{draws}{A matrix or data frame of draws, with one row per draw and one
column per reported quantity. More draws than columns are needed, or the
covariance matrix is singular.}

\item{study}{A string naming the study the draws come from.}

\item{type}{A character vector giving what each column of \code{draws} is, one of
\code{"mean"}, \code{"sd"} or \code{"quantile"} per column. Defaults to \code{NULL}, which uses
the column names of \code{draws} where all of them are already supported types.}

\item{p}{A numeric vector of probabilities for the columns with a \code{type} of
\code{"quantile"}. Either one entry per quantile column, or one per column with
the entries for other types ignored. Required when any column is a
quantile.}

\item{value}{A numeric vector of reported values, one per column of \code{draws}.
Defaults to \code{NULL}, which reports the mean of each column. Supply it where
the study would publish a point estimate computed some other way, such as
the summary of the delays a resampling procedure started from.}

\item{...}{Further columns to attach to every returned row, such as the
sample size \code{n} and the study metadata documented in
\code{\link{as_epidist_estimates_data}()}.}
}
\value{
A list with a \code{data} frame of reported summaries and a \code{vcov} list
holding the covariance matrix, both ready to pass to
\code{\link{as_epidist_estimates_data}()}.
}
\description{
Turns draws of the quantities a study reports into the summary vector and
covariance matrix that \code{\link{as_epidist_estimates_data}()} takes. Rows are draws
and columns are reported quantities, so anything able to produce draws of
what a study would report can be used.
}
\details{
Posterior draws from a model already fitted to the delays are the common
case, and \code{\link{predict_delay_parameters}()} returns them for a fit made with this
package. Its \code{mean} and \code{sd} columns are draws of exactly the quantities a
study reports. It returns one row per draw for each row of the data the fit
used, so take a single \code{index} before passing them in, or supply the
\code{newdata} the summary should describe. Resampling a set of delays is another
way to produce draws, and \code{\link{delays_to_multivariate}()} wraps that.

The reported vector is the mean of each column and the covariance is the
covariance of the draws, so the correlation between the reported quantities
is kept. Reporting a standard error for each summary on its own throws that
correlation away, and a meta analysis of the published numbers then loses
more than it needs to.

The number of delays the study summarised is not taken from the draws,
because draws carry no such count. Supply it, and any other study metadata,
through \code{...}. The reporting table of Charniga et al. (2024) covers most of
what is needed, and passing those fields through \code{...} puts them straight
onto the returned rows. A study reporting a covariance matrix needs no sample
size, because the matrix already gives the uncertainty of every summary.
}
\examples{
set.seed(1)
draws <- cbind(mean = rnorm(500, 7.5, 0.3), sd = rnorm(500, 3.6, 0.2))
reported <- draws_to_multivariate(draws, study = "site A", n = 200)
as_epidist_estimates_data(reported$data, vcov = reported$vcov)
}
\seealso{
Other estimates_data: 
\code{\link{as_epidist_estimates_data}()},
\code{\link{as_epidist_estimates_data.data.frame}()},
\code{\link{assert_epidist.epidist_estimates_data}()},
\code{\link{delays_to_multivariate}()},
\code{\link{is_epidist_estimates_data}()},
\code{\link{new_epidist_estimates_data}()},
\code{\link{parameters_to_multivariate}()}
}
\concept{estimates_data}
