% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/newdata.R
\name{epidist_newdata.epidist_meta_model}
\alias{epidist_newdata.epidist_meta_model}
\title{Build \code{newdata} for the meta model}
\usage{
\method{epidist_newdata}{epidist_meta_model}(
  data,
  ...,
  pwindow = 0,
  swindow = 0,
  relative_obs_time = Inf,
  delay_min = 0
)
}
\arguments{
\item{data}{An \code{epidist} data object, such as one returned by
\code{\link[=as_epidist_marginal_model]{as_epidist_marginal_model()}}, \code{\link[=as_epidist_latent_model]{as_epidist_latent_model()}},
\code{\link[=as_epidist_naive_model]{as_epidist_naive_model()}} or \code{\link[=as_epidist_meta_model]{as_epidist_meta_model()}}.}

\item{...}{Variables to expand into a grid, passed to \code{\link[tidyr:expand]{tidyr::expand()}}.
Supply the variables used in the model formula, such as \code{sex}. Each
combination of their unique values becomes a row. Supply no variables to
get a single row, which is what an intercept only model needs. A variable
expanded here keeps its expanded values, so naming it as an argument of
the method as well is an error.}

\item{pwindow}{Width of the primary event censoring window. Defaults to 0,
which is no censoring.}

\item{swindow}{Width of the secondary event censoring window. Defaults to 0,
which is no censoring.}

\item{relative_obs_time}{Observation time relative to the primary event.
Defaults to \code{Inf}, which is no right truncation. \code{brms} warns about
infinite values in the data when this is \code{Inf}. That warning is safe here,
because prediction is done in R and never passes the value to Stan.}

\item{delay_min}{Minimum delay, the left truncation point. Defaults to 0,
which is no left truncation.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of \code{newdata} ready to predict from.
}
\description{
The meta model holds individual level rows and summary rows in one data
frame, telling them apart by the \code{obs_type} slot. Prediction is for the
delay distribution itself, so this method builds an individual level row.
It takes the same arguments as \code{\link[=epidist_newdata.epidist_marginal_model]{epidist_newdata.epidist_marginal_model()}},
and fills the slots a summary row would use with the values an individual
row carries. The result is what \code{\link[=delay_parameter_draws]{delay_parameter_draws()}} and
\code{\link[=add_summaries]{add_summaries()}} need, so there is no need to copy a summary row out of
the model data and overwrite its covariates.
}
\details{
A model with a study level term, such as \code{mu ~ 1 + (1 | study)}, needs
either a \code{study} column in \code{newdata} or \code{re_formula = NA} when predicting.
Expand \code{study} here to predict for each study, or leave it out and pass
\code{re_formula = NA} for the population level delay. The primary event
distribution of the individual level rows is a parameter of the model
rather than a column, so it needs nothing here.
}
\examples{
estimates <- as_epidist_estimates_data(
  data.frame(
    study = c("A", "A", "B"),
    type = c("mean", "sd", "mean"),
    value = c(7.5, 3.6, 6.4),
    n = c(120, 120, 80),
    relative_obs_time = c(20, 20, Inf),
    trunc_adjusted = c(FALSE, FALSE, TRUE),
    cens_adjusted = c(0, 0, 1)
  )
)
meta <- as_epidist_meta_model(estimates = estimates)

# The population level delay, with no censoring and no truncation
epidist_newdata(meta)

# A row for each study, with daily censoring
epidist_newdata(meta, study, pwindow = 1, swindow = 1)
}
\seealso{
Other meta_model: 
\code{\link{as_epidist_meta_model}()},
\code{\link{as_epidist_meta_model.NULL}()},
\code{\link{as_epidist_meta_model.epidist_aggregate_data}()},
\code{\link{as_epidist_meta_model.epidist_estimates_data}()},
\code{\link{as_epidist_meta_model.epidist_linelist_data}()},
\code{\link{assert_epidist.epidist_meta_model}()},
\code{\link{epidist_family_model.epidist_meta_model}()},
\code{\link{epidist_formula_model.epidist_meta_model}()},
\code{\link{epidist_model_prior.epidist_meta_model}()},
\code{\link{epidist_transform_data_model.epidist_meta_model}()},
\code{\link{is_epidist_meta_model}()},
\code{\link{new_epidist_meta_model}()}

Other newdata: 
\code{\link{epidist_newdata}()},
\code{\link{epidist_newdata.default}()},
\code{\link{epidist_newdata.epidist_latent_model}()},
\code{\link{epidist_newdata.epidist_marginal_model}()},
\code{\link{epidist_newdata.epidist_naive_model}()}
}
\concept{meta_model}
\concept{newdata}
