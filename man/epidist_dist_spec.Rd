% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_spec.R
\name{epidist_dist_spec}
\alias{epidist_dist_spec}
\alias{as_dist_spec.epidist_fit}
\title{Export a fitted delay distribution as a \code{distspec} distribution}
\usage{
epidist_dist_spec(
  object,
  newdata = NULL,
  representation = c("independent", "joint"),
  max = Inf,
  cdf_max = 1,
  ...
)

\method{as_dist_spec}{epidist_fit}(x, ...)
}
\arguments{
\item{object}{A model fit with \code{\link[=epidist]{epidist()}}.}

\item{newdata}{A \code{data.frame} of data to predict for, with one row per
delay distribution wanted. If \code{NULL}, the default, \code{\link[=epidist_newdata]{epidist_newdata()}}
builds one row per unique combination of the predictors, with no
censoring and no truncation. See the details.}

\item{representation}{Either \code{"independent"}, the default, which gives
each natural parameter its own \code{\link[distspec:Normal]{distspec::Normal()}} prior, or \code{"joint"},
which gives them a single \code{MultiNormal()} prior. See the details.}

\item{max}{The maximum of the delay distribution, passed to
\code{\link[distspec:bound_dist]{distspec::bound_dist()}}. Defaults to \code{Inf}, which is no maximum.}

\item{cdf_max}{The cumulative probability to keep the delay distribution
up to, passed to \code{\link[distspec:bound_dist]{distspec::bound_dist()}}. Defaults to 1, which keeps the
whole distribution.}

\item{...}{Additional arguments passed to \code{\link[brms:prepare_predictions]{brms::prepare_predictions()}},
such as \code{ndraws} or \code{re_formula}.}

\item{x}{A model fit with \code{\link[=epidist]{epidist()}}.}
}
\value{
A \verb{<dist_spec>} when \code{newdata} has one row. A named list of them,
one per row of \code{newdata}, when it has several. The names give the values
of the columns of \code{newdata} that differ between rows, such as \code{"sex=0"},
and are the row numbers when no column differs.
}
\description{
Summarises the posterior of a fitted delay distribution into an uncertain
\verb{<dist_spec>} from the \code{distspec} package, for use in packages that take
their delay distributions in that form. The natural parameters of the
delay distribution are computed for each posterior draw and summarised
into a prior on each of them. The \code{distspec} package is not required to
install \code{epidist}, so install it separately to use this.

\code{epidist_dist_spec()} is also registered as the \code{distspec::as_dist_spec()}
method for fitted models, so \code{as_dist_spec(fit)} gives the same result
once \code{distspec} is loaded.
}
\details{
The lognormal, gamma and Weibull families are supported. Their \code{brms}
parameters are mapped to the natural parameters of the matching \code{distspec}
constructor for every draw before summarising:
\itemize{
\item lognormal: \code{mu} is \code{meanlog} and \code{sigma} is \code{sdlog} of
\code{\link[distspec:LogNormal]{distspec::LogNormal()}}.
\item gamma: \code{shape} is \code{shape} and \code{shape / mu} is \code{rate} of
\code{\link[distspec:Gamma]{distspec::Gamma()}}.
\item Weibull: \code{shape} is \code{shape} and \code{mu / gamma(1 + 1 / shape)} is \code{scale}
of \code{\link[distspec:Weibull]{distspec::Weibull()}}.
}

With \code{representation = "independent"}, the default, each natural
parameter gets a \code{\link[distspec:Normal]{distspec::Normal()}} prior with the mean and standard
deviation of its marginal posterior. The posterior correlation between the
parameters is not represented, so sampling from the result gives a wider
range of delay distributions than the posterior does.

With \code{representation = "joint"}, the natural parameters get a single
\code{MultiNormal()} prior built from the posterior mean vector and covariance
matrix, which keeps the correlation. This needs a version of \code{distspec}
that provides \code{MultiNormal()}, which \code{distspec} 0.2.0 does not. The
function errors when it is not available rather than falling back, so the
representation of a result is always the one asked for.

The default \code{newdata} is built with \code{\link[=epidist_newdata]{epidist_newdata()}} by expanding the
variables in the model formula into a grid, so it has one row per unique
combination of the predictors, and gives the delay distribution with no
censoring and no truncation. A model with only an intercept gets a single
row. A continuous predictor gets a row per distinct value, so pass
\code{newdata} for such a model.
}
\examples{
\dontshow{if (requireNamespace("distspec", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
\donttest{
fit <- sierra_leone_ebola_data |>
  as_epidist_linelist_data(
    pdate_lwr = "date_of_symptom_onset",
    sdate_lwr = "date_of_sample_tested"
  ) |>
  as_epidist_aggregate_data() |>
  as_epidist_marginal_model() |>
  epidist(chains = 2, cores = 2, refresh = ifelse(interactive(), 250, 0))

dist <- epidist_dist_spec(fit, max = 60)
dist

# The delay distribution at the posterior mean of its parameters
distspec::fix_parameters(dist, strategy = "mean")

# The same through the distspec generic
distspec::as_dist_spec(fit)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=delay_parameter_draws]{delay_parameter_draws()}} for the draws this summarises, and
\code{\link[=epidist_newdata]{epidist_newdata()}} to build \code{newdata}.

Other postprocess: 
\code{\link{add_summaries}()},
\code{\link{delay_parameter_draws}()},
\code{\link{delay_summary_draws}()},
\code{\link{epidist_strata}()}
}
\concept{postprocess}
