% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{delay_parameter_draws}
\alias{delay_parameter_draws}
\alias{add_delay_parameter_draws}
\title{Posterior draws of the delay distribution parameters}
\usage{
delay_parameter_draws(object, newdata = NULL, ...)

add_delay_parameter_draws(newdata, object, ...)
}
\arguments{
\item{object}{A model fit with \code{\link[=epidist]{epidist()}}.}

\item{newdata}{A \code{data.frame} of data to predict for. If \code{NULL}, the default,
the data the model was fitted to is used. The \code{brms} models \code{epidist} fits
need the model variables as well as the predictors, so build \code{newdata} with
\code{\link[=epidist_strata]{epidist_strata()}} rather than from the predictors alone.}

\item{...}{Additional arguments passed to \code{\link[brms:prepare_predictions]{brms::prepare_predictions()}},
such as \code{ndraws} or \code{re_formula}.}
}
\value{
A \code{tibble} of posterior draws of the delay distribution parameters,
grouped by the columns of \code{newdata} and by \code{.row}.
}
\description{
Returns posterior draws of the parameters of the delay distribution in the
long format used by \code{tidybayes}. The delay parameters are the distributional
parameters of the \code{brms} family, evaluated on the response scale for each
row of \code{newdata}. For a lognormal model they are \code{mu} and \code{sigma}. They are
the parameters of the delay distribution itself, so they do not describe the
censoring or truncation of the observation process, and they are not the
natural scale mean and standard deviation of the delay. Use
\code{\link[=add_summaries]{add_summaries()}} to add those.

\code{add_delay_parameter_draws()} is the same function with \code{newdata} first, for
use at the start of a pipeline as with \code{\link[tidybayes:add_predicted_draws]{tidybayes::add_epred_draws()}}.
}
\details{
The returned columns follow the \code{tidybayes} conventions. The columns of
\code{newdata} come first, followed by \code{.row}, \code{.chain}, \code{.iteration} and
\code{.draw}, followed by one column per distributional parameter. The result is
grouped by the columns of \code{newdata} and by \code{.row}. \code{.chain} and \code{.iteration}
are \code{NA} when the draws have been subset, because the chain a subset draw
came from is not recoverable.

Every row of \code{newdata} gets its own draws, so passing the data the model was
fitted to produces many identical draws when the model has few unique
combinations of predictors. \code{\link[=epidist_strata]{epidist_strata()}} returns one row per unique
combination and is usually the better input.
}
\examples{
\donttest{
fit <- sierra_leone_ebola_data |>
  as_epidist_linelist_data(
    pdate_lwr = "date_of_symptom_onset",
    sdate_lwr = "date_of_sample_tested"
  ) |>
  as_epidist_aggregate_data() |>
  as_epidist_marginal_model() |>
  epidist(chains = 2, cores = 2, refresh = ifelse(interactive(), 250, 0))

fit |>
  epidist_strata() |>
  add_delay_parameter_draws(fit) |>
  add_summaries(probs = c(0.05, 0.95))
}
}
\seealso{
\code{\link[=add_summaries]{add_summaries()}} to add natural scale summaries of the delay,
\code{\link[=epidist_strata]{epidist_strata()}} to build \code{newdata}.

Other postprocess: 
\code{\link{add_summaries}()},
\code{\link{epidist_strata}()}
}
\concept{postprocess}
