% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_implied_prob}
\alias{.meta_implied_prob}
\title{The cumulative probability a study using a given procedure would report}
\usage{
.meta_implied_prob(
  y,
  dist,
  args,
  lower = 0,
  cutoff,
  pwindow,
  swindow,
  trunc_adjusted,
  cens_adjusted,
  growth_rate,
  trunc_design = 0L
)
}
\arguments{
\item{y}{The reported quantile value.}

\item{dist}{A \code{primarycensored} distribution function name.}

\item{args}{A named list of distribution parameters.}

\item{lower}{The study's minimum delay (its left truncation point).}

\item{cutoff}{The grid cutoff, either the study observation time or
\code{max_delay}.}

\item{pwindow, swindow}{The primary and secondary censoring window widths.}

\item{trunc_adjusted}{1 if the study adjusted for right truncation, 0
otherwise.}

\item{cens_adjusted}{The censoring adjustment code, one of 0, 1, 2, 3, or
4.}

\item{growth_rate}{The exponential growth rate of primary events.}

\item{trunc_design}{0 for a cohort design, 1 for an accrual design.}
}
\value{
A probability, or \code{Inf} if the distribution function underflows
to zero at \code{cutoff}, which forces a \code{-Inf} log likelihood rather than a
\code{NaN} one.
}
\description{
Evaluates the distribution function of the biased estimand at a reported
quantile value. Working on the probability scale avoids inverting the
distribution function, which has no closed form on the discrete grid.
}
\details{
For a naive study (\code{cens_adjusted} of 0) the estimand is discrete, so the
continuity corrected grid distribution function of \code{\link[=.meta_grid_prob]{.meta_grid_prob()}} is
used. Midpoint imputation (\code{cens_adjusted} of 3) uses the same function
evaluated half a secondary window lower, because the study shifted every
delay up by that amount.

For the uniform single interval approximation (\code{cens_adjusted} of 2) the
distribution function of the primary censored delay is used, so that it
matches the moments used for reported means and standard deviations.
Midpoint imputation with a uniform interval (\code{cens_adjusted} of 4) uses that
function evaluated half a primary window higher, because the study anchored
every delay at the centre of the primary window rather than at its lower
edge.
}
\keyword{internal}
