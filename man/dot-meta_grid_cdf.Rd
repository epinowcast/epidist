% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_grid_cdf}
\alias{.meta_grid_cdf}
\title{The uncorrected grid distribution function at a set of cells}
\usage{
.meta_grid_cdf(
  cell,
  dist,
  args,
  lower = 0,
  cutoff,
  pwindow,
  swindow,
  growth_rate,
  accrual = 0L
)
}
\arguments{
\item{cell}{An integer vector of grid cell indices, counting from zero.}

\item{dist}{A \code{primarycensored} distribution function name.}

\item{args}{A named list of distribution parameters.}

\item{lower}{The study's minimum delay (its left truncation point).}

\item{cutoff}{The grid cutoff, either the study observation time or
\code{max_delay}.}

\item{pwindow, swindow}{The primary and secondary censoring window widths.}

\item{growth_rate}{The exponential growth rate of primary events.}

\item{accrual}{1 to apply the accrual weight, 0 otherwise.}
}
\value{
The grid distribution function at each cell, or infinities if
the grid mass underflows to zero.
}
\description{
The number of delays a study saw at or below a grid cell is binomial on
the grid distribution function at that cell, which is the mass of the
cells up to and including it. A cohort grid is normalised by the mass it
holds, so only the cells asked for, the top of the grid and its first cell
are evaluated, as in \code{\link[=.meta_grid_edges]{.meta_grid_edges()}}. An accrual grid reweights every
cell before renormalising, so it is built in full by \code{\link[=.meta_grid_pmf]{.meta_grid_pmf()}}.
}
\keyword{internal}
