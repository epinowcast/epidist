% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_delays.R
\name{.fitted_delays}
\alias{.fitted_delays}
\title{Take the observed delays of a fitted model}
\usage{
.fitted_delays(x, by = NULL)
}
\arguments{
\item{x}{An \code{epidist_linelist_data} or \code{epidist_aggregate_data} object, a
named list of them to compare, or a model fitted with \code{\link[=epidist]{epidist()}}.}

\item{by}{A character vector of columns of the model data that define the
strata to colour by. If \code{NULL}, the default, the variables in the
distributional parameter formulas are used, as \code{\link[=epidist_strata]{epidist_strata()}} does.}
}
\value{
A list with \code{data}, a \code{tibble} of the observed \code{delay} of each
case, its weight \code{n} and the columns in \code{by}, and \code{by}, the columns the
strata are defined by or \code{NULL} when there are none.
}
\description{
Reads the response and the case weights of the model data, and the strata
to colour by, which default to the variables in the distributional
parameter formulas as they do in \code{\link[=epidist_strata]{epidist_strata()}}.
}
\keyword{internal}
