% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior.R
\name{epidist_model_prior.epidist_meta_model}
\alias{epidist_model_prior.epidist_meta_model}
\title{Model specific prior distributions for the meta model}
\usage{
\method{epidist_model_prior}{epidist_meta_model}(data, formula, ...)
}
\arguments{
\item{data}{An object with class corresponding to an implemented model.}

\item{formula}{An object of class \link[stats:formula]{stats::formula} or \link[brms:brmsformula]{brms::brmsformula}
(or one that can be coerced to those classes). A symbolic description of
the model to be fitted. A formula must be provided for the distributional
parameter \code{mu}, and may optionally be provided for other distributional
parameters.}

\item{...}{Additional arguments passed to \code{fn} method.}
}
\value{
A \code{brmsprior} object, or \code{NULL} when the model adds no priors.
}
\description{
The response column of a meta model is a placeholder on every summary row,
and \code{brms} centres its default prior for the intercept of \code{mu} on the
response. For a model fitted to summaries alone that default is centred on
a delay of zero. This method puts a \code{normal(1, 1)} prior on the intercept
instead, the scale of the lognormal family prior in
\code{\link[=epidist_family_prior]{epidist_family_prior()}}, so that a Gamma or Weibull meta fit gets a prior
on the same scale as a lognormal one. On the log scale it is a median
delay of about 3 days with a 95\% range of roughly 0.4 to 20 days.
}
\details{
The centre is fixed rather than taken from the reported values, because a
prior chosen from the data is not a prior. It would put the posterior of a
small review where the data already sit and understate how much the
studies disagree. The prior is added where \code{mu} is on the log scale, which
is the lognormal family, whose \code{mu} is the log of the median under an
identity link, and any family with a log link. Nothing is added for other
links, and a model with individual level rows only adds no prior, so the
family or \code{brms} default applies as it does for the marginal model.

The between study spread of any group level term, such as \code{(1 | study)},
gets a half normal prior with a standard deviation of 0.25 on the scale of
the linear predictor, so that a small review cannot fit that spread from
almost nothing under the wide \code{brms} default. It is dropped where the
formula has no group level term. The prior on the intercept of the other
distributional parameters is left to the family or to \code{brms}.
}
\seealso{
Other prior: 
\code{\link{epidist_family_prior}()},
\code{\link{epidist_family_prior.default}()},
\code{\link{epidist_family_prior.lognormal}()},
\code{\link{epidist_model_prior}()},
\code{\link{epidist_model_prior.default}()},
\code{\link{epidist_prior}()}

Other meta_model: 
\code{\link{as_epidist_meta_model}()},
\code{\link{as_epidist_meta_model.NULL}()},
\code{\link{as_epidist_meta_model.epidist_aggregate_data}()},
\code{\link{as_epidist_meta_model.epidist_estimates_data}()},
\code{\link{as_epidist_meta_model.epidist_linelist_data}()},
\code{\link{assert_epidist.epidist_meta_model}()},
\code{\link{epidist_family_model.epidist_meta_model}()},
\code{\link{epidist_formula_model.epidist_meta_model}()},
\code{\link{epidist_newdata.epidist_meta_model}()},
\code{\link{epidist_transform_data_model.epidist_meta_model}()},
\code{\link{is_epidist_meta_model}()},
\code{\link{new_epidist_meta_model}()}
}
\concept{meta_model}
\concept{prior}
