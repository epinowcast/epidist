% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_box_step}
\alias{.meta_box_step}
\title{One step of the forward pass over the cumulative counts}
\usage{
.meta_box_step(alpha, a, b, a2, b2, r, study_n, m_prev, m, lg)
}
\arguments{
\item{alpha}{The log probabilities of the source counts \code{a:b}.}

\item{a, b}{The first and last source count.}

\item{a2, b2}{The first and last target count.}

\item{r}{The step probability from \code{\link[=.meta_step_prob]{.meta_step_prob()}}.}

\item{study_n}{The number of delays the quantiles were computed from.}

\item{m_prev, m}{The most likely source and target counts from
\code{\link[=.meta_box_mode_path]{.meta_box_mode_path()}}.}

\item{lg}{The log factorials, \code{lg[d + 1]} being \verb{log(d!)}.}
}
\value{
The log probabilities of the target counts \code{a2:b2}.
}
\description{
Carries the log probabilities of the counts in one band to the counts in
the next, through the binomial step of the chain. The step is a matrix
product of \code{\link[=.meta_step_kernel]{.meta_step_kernel()}} with the exponentiated source vector,
each scaled so that the arithmetic stays in range, and a sum on the log
scale over every pair of counts where the kernel would underflow at the
most likely step. Both give the same probabilities. Matches
\code{meta_family_box_step()} in Stan.
}
\keyword{internal}
