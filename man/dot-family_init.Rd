% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{.family_init}
\alias{.family_init}
\title{Initial values a family starts its fits from}
\usage{
.family_init(family)
}
\arguments{
\item{family}{A description of the response distribution and link function to
be used in the model. Every family function has a link argument allowing
users to specify the link function to be applied on the response variable.
If not specified, default links are used. For details of all supported
families see \code{\link[brms:brmsfamily]{brms::brmsfamily()}}. Commonly used, such as
\code{\link[brms:lognormal]{brms::lognormal()}}, are also
reexported as part of \code{epidist}. The generalised gamma family
\code{\link[=gengamma]{gengamma()}} is defined by \code{epidist} itself.}
}
\value{
A function returning a named list of initial values, as taken by
the \code{init} argument of \code{\link[brms:brm]{brms::brm()}}, or \code{NULL}.
}
\description{
Stan draws the initial value of every parameter uniformly between -2 and 2
on the unconstrained scale. For the \code{\link[=gengamma]{gengamma()}} family that includes
starting points where \code{shape} and \code{k} are both small. There the
distribution is so heavy tailed that the sampler adapts to a tiny step size
and does not reach the bulk of the posterior. Its fits therefore start the
intercepts of \code{shape} and \code{k} at one, the gamma and Weibull special case,
and draw every other parameter as usual. Other families use the defaults
of the fitting function.
}
\keyword{internal}
