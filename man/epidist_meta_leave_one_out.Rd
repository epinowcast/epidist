% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leave_one_out.R
\name{epidist_meta_leave_one_out}
\alias{epidist_meta_leave_one_out}
\title{Refit a meta model leaving each study out in turn}
\usage{
epidist_meta_leave_one_out(
  fit,
  data = NULL,
  newdata = NULL,
  re_formula = NA,
  width = 0.95,
  keep_fits = FALSE,
  ...
)
}
\arguments{
\item{fit}{A meta model fitted with \code{\link[=epidist]{epidist()}} to an \code{epidist_meta_model}
object.}

\item{data}{The \code{epidist_meta_model} object \code{fit} was fitted to.
Only needed when \code{fit$data} has no \code{study} column, which happens when the
formula does not use \code{study}.
If \code{NULL}, the default, the model data stored in \code{fit} is used.}

\item{newdata}{A \code{data.frame} of data to predict the delay for, passed to
\code{\link[=delay_summary_draws]{delay_summary_draws()}}.
If \code{NULL}, the default, a single row giving the population level delay
with no censoring and no truncation is built with \code{\link[=epidist_newdata]{epidist_newdata()}}.}

\item{re_formula}{Passed to \code{\link[brms:prepare_predictions]{brms::prepare_predictions()}} through
\code{\link[=delay_summary_draws]{delay_summary_draws()}}.
The default \code{NA} switches off any study level term, so the summaries are
of the population level delay.}

\item{width}{The width of the central posterior intervals.
Defaults to \code{0.95}.}

\item{keep_fits}{If \code{TRUE}, the refits are returned in the \code{fits} attribute
of the result as a list named by the held out study.
Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[brms:update.brmsfit]{brms::update.brmsfit()}} and so
to \code{\link[brms:brm]{brms::brm()}}, such as \code{cores}, \code{chains}, \code{iter}, \code{refresh} and
\code{silent}.}
}
\value{
A \code{tibble} with one row per held out study, row of \code{newdata} and
summary (\code{"mean"} or \code{"sd"}).
It gives the posterior median (\code{estimate}) and interval (\code{lower},
\code{upper}) of the refit and of the full fit (\verb{full_*}), and the
\code{difference} from the full fit with its interval.
Predictors of the model in \code{newdata} other than \code{study} are included.
The \code{width} attribute records the interval width.
}
\description{
Refits a meta model once per study with that study removed, and compares
the delay mean and standard deviation of each refit with the full fit.
}
\details{
Individual level rows are held out together as the \code{"individual"} study.
If the formula does not use \code{study}, pass the \code{epidist_meta_model} object
as \code{data}, because \code{brms} drops unused variables from \code{fit$data}.
Each refit reuses the compiled model through \code{\link[brms:update.brmsfit]{brms::update.brmsfit()}}.
The refits are exact because Pareto smoothed importance sampling
(Vehtari et al. 2017) is often unreliable when a whole study is left out.

The comparison is reported as estimates with intervals and no threshold,
as in \code{marginaleffects} (Arel-Bundock et al. 2024).
\code{difference} is the refit posterior minus the full fit's posterior median.
The two fits share most of their data, so their draws are not differenced.
}
\examples{
\donttest{
estimates <- as_epidist_estimates_data(
  data.frame(
    study = c("A", "A", "B", "B", "C", "C"),
    type = c("mean", "sd", "mean", "sd", "mean", "sd"),
    value = c(7.5, 3.6, 6.4, 3.1, 8.2, 4.0),
    n = c(120, 120, 80, 80, 150, 150),
    relative_obs_time = c(20, 20, 25, 25, Inf, Inf),
    trunc_adjusted = c(FALSE, FALSE, FALSE, FALSE, TRUE, TRUE),
    cens_adjusted = c(0, 0, 0, 0, 1, 1)
  )
)
meta <- as_epidist_meta_model(estimates = estimates)
fit <- epidist(
  meta,
  chains = 2, cores = 2, refresh = ifelse(interactive(), 250, 0)
)

# The formula does not use study, so pass the model data
epidist_meta_leave_one_out(fit, data = meta, refresh = 0)
}
}
\references{
\itemize{
\item Vehtari et al. (2017) \doi{10.1007/s11222-016-9696-4}
\item Arel-Bundock et al. (2024) \doi{10.18637/jss.v111.i09}
}
}
\seealso{
\code{\link[=delay_summary_draws]{delay_summary_draws()}} for the summaries the comparison uses and
\code{\link[=epidist_newdata]{epidist_newdata()}} to build \code{newdata}.

Other meta_model:
\code{\link[=as_epidist_meta_model]{as_epidist_meta_model()}},
\code{\link[=as_epidist_meta_model.NULL]{as_epidist_meta_model.NULL()}},
\code{\link[=as_epidist_meta_model.epidist_aggregate_data]{as_epidist_meta_model.epidist_aggregate_data()}},
\code{\link[=as_epidist_meta_model.epidist_estimates_data]{as_epidist_meta_model.epidist_estimates_data()}},
\code{\link[=as_epidist_meta_model.epidist_linelist_data]{as_epidist_meta_model.epidist_linelist_data()}},
\code{\link[=assert_epidist.epidist_meta_model]{assert_epidist.epidist_meta_model()}},
\code{\link[=epidist_family_model.epidist_meta_model]{epidist_family_model.epidist_meta_model()}},
\code{\link[=epidist_formula_model.epidist_meta_model]{epidist_formula_model.epidist_meta_model()}},
\code{\link[=epidist_model_prior.epidist_meta_model]{epidist_model_prior.epidist_meta_model()}},
\code{\link[=epidist_newdata.epidist_meta_model]{epidist_newdata.epidist_meta_model()}},
\code{\link[=epidist_transform_data_model.epidist_meta_model]{epidist_transform_data_model.epidist_meta_model()}},
\code{\link[=is_epidist_meta_model]{is_epidist_meta_model()}},
\code{\link[=new_epidist_meta_model]{new_epidist_meta_model()}}
}
\concept{meta_model}
