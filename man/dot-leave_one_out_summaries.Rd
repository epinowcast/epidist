% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leave_one_out.R
\name{.leave_one_out_summaries}
\alias{.leave_one_out_summaries}
\title{Posterior summaries of the delay mean and standard deviation of a fit}
\usage{
.leave_one_out_summaries(fit, newdata, re_formula = NA, width)
}
\arguments{
\item{fit}{A meta model fitted with \code{\link[=epidist]{epidist()}} to an \code{epidist_meta_model}
object.}

\item{newdata}{A \code{data.frame} of data to predict the delay for, passed to
\code{\link[=delay_summary_draws]{delay_summary_draws()}}.
Always an already resolved \code{data.frame}, never \code{NULL}.}

\item{re_formula}{Passed to \code{\link[brms:prepare_predictions]{brms::prepare_predictions()}} through
\code{\link[=delay_summary_draws]{delay_summary_draws()}}.
The default \code{NA} switches off any study level term, so the summaries are
of the population level delay.}

\item{width}{The width of the central posterior interval reported for each
summary, and used to flag influential studies.
Defaults to \code{0.95}.}
}
\value{
A \code{tibble} with one row per row of \code{newdata} and summary, with
columns \code{.row}, \code{summary}, \code{estimate} (the posterior median), \code{lower},
\code{upper} and \code{posterior_sd}.
}
\description{
Posterior summaries of the delay mean and standard deviation of a fit
}
\keyword{internal}
