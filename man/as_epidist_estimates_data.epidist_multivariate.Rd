% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_data.R
\name{as_epidist_estimates_data.epidist_multivariate}
\alias{as_epidist_estimates_data.epidist_multivariate}
\title{Create an \code{epidist_estimates_data} object from a multivariate representation}
\usage{
\method{as_epidist_estimates_data}{epidist_multivariate}(
  data,
  study,
  family = NULL,
  moments = c("mean", "sd"),
  probs = numeric(0),
  mvn_id = NULL,
  ...
)
}
\arguments{
\item{data}{The data to convert}

\item{study}{A string naming the study the draws come from.}

\item{family}{The distribution the study fitted, one of \code{"lognormal"},
\code{"gamma"} or \code{"weibull"}, where the draws hold its natural parameters.
Defaults to \code{NULL}, meaning the draws already hold reported summaries.}

\item{moments}{Which moments to report, any of \code{"mean"} and \code{"sd"}. Only
used where \code{family} is given.}

\item{probs}{A numeric vector of probabilities to report quantiles at. Only
used where \code{family} is given.}

\item{mvn_id}{A string identifying the covariance matrix. Defaults to
\code{study}, and only needs setting where one study contributes more than one
multivariate object.}

\item{...}{Study metadata, as documented in
\code{\link[=as_epidist_estimates_data.data.frame]{as_epidist_estimates_data.data.frame()}}.}
}
\description{
This is the only route by which a covariance between a study's reported
summaries reaches \code{\link[=as_epidist_meta_model]{as_epidist_meta_model()}}. The covariance comes from draws
of the study's parameters, so it is one the study could have computed, and
the summaries it covers are fitted with a multivariate normal likelihood
rather than as independent observations.
}
\details{
Where \code{family} is given, the draws hold the natural parameters of a
distribution the study fitted. Each draw is pushed through to the summaries
the fitted distribution implies, over the range of delays the study could
have seen, and the covariance is taken over those. This is the exact version
of the delta method \code{\link[=epidist_estimates_parameters]{epidist_estimates_parameters()}} applies, and it needs
no linearisation.

Where \code{family} is \code{NULL}, the parameters must already be quantities a study
reports. Name them \code{mean}, \code{sd}, or \code{q} followed by a probability, such as
\code{q0.25}.

Draws over more than one index point are not yet supported for fitting,
because the linear predictor would have to vary within one likelihood
observation.
}
\examples{
set.seed(1)
draws <- cbind(mean = rnorm(500, 7.5, 0.3), sd = rnorm(500, 3.6, 0.2))
as_epidist_estimates_data(
  as_epidist_multivariate(draws),
  study = "site A"
)
}
\seealso{
Other estimates_data:
\code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}},
\code{\link[=as_epidist_estimates_data.data.frame]{as_epidist_estimates_data.data.frame()}},
\code{\link[=as_epidist_estimates_data.epidist_estimates_data]{as_epidist_estimates_data.epidist_estimates_data()}},
\code{\link[=as_epidist_estimates_data.list]{as_epidist_estimates_data.list()}},
\code{\link[=assert_epidist.epidist_estimates_data]{assert_epidist.epidist_estimates_data()}},
\code{\link[=epidist_estimates_parameters]{epidist_estimates_parameters()}},
\code{\link[=epidist_estimates_summaries]{epidist_estimates_summaries()}},
\code{\link[=is_epidist_estimates_data]{is_epidist_estimates_data()}},
\code{\link[=new_epidist_estimates_data]{new_epidist_estimates_data()}}
}
\concept{estimates_data}
