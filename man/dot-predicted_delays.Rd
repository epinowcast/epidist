% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_delays.R
\name{.predicted_delays}
\alias{.predicted_delays}
\title{Bin the delays a fitted model predicts for each case}
\usage{
.predicted_delays(x, delays, binwidth = 1, ndraws = 100, probs = c(0.05, 0.95))
}
\arguments{
\item{x}{An \code{epidist_linelist_data} or \code{epidist_aggregate_data} object, a
named list of them to compare, or a model fitted with \code{\link[=epidist]{epidist()}}.}

\item{delays}{A \code{tibble} of the observed delays with their \code{.stratum}, as
returned by \code{\link[=.draw_strata]{.draw_strata()}}.}

\item{binwidth}{The width of the delay bins, on the scale of the event
times. Defaults to 1, the daily censoring the data usually has.}

\item{ndraws}{The number of posterior draws to predict from, sampled at
random. Defaults to 100, which is enough for the median and the
quantiles of a binned distribution and bounds the size of the
prediction. Use \code{NULL} to predict from every draw.}

\item{probs}{A numeric vector of two probabilities giving the quantiles the
ribbon spans. Defaults to \code{c(0.05, 0.95)}.}
}
\value{
A \code{tibble} with one row per stratum and bin, holding the lower
edge of the bin as \code{delay}, the posterior median \code{density} and the
\code{lower} and \code{upper} quantiles of it.
}
\description{
Predicts the delay of every case in the model data from each posterior
draw, bins the predictions of each draw as \code{\link[=.bin_delays]{.bin_delays()}} bins the
observed delays, and summarises the proportion in each bin across the
draws. Draws with no prediction in a bin count as a proportion of zero.
}
\keyword{internal}
