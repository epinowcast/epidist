% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_log_accrual_weight}
\alias{.meta_log_accrual_weight}
\title{The log follow up available to a delay under an accrual design}
\usage{
.meta_log_accrual_weight(d, window, growth_rate)
}
\arguments{
\item{d}{A numeric vector of delays.}

\item{window}{The length of the collection window.}

\item{growth_rate}{The exponential growth rate of primary events.}
}
\value{
A numeric vector of log follow up times.
}
\description{
A study that collected primary events over a window of length \code{window} and
stopped at its calendar end could only observe a delay of \code{d} for the
primary events that occurred at least \code{d} before the stop. With primary
events arriving at a rate proportional to \eqn{\exp(r t)} the amount of such
follow up is
\eqn{w(d) = \int_0^{window - d} \exp(r t) \text{d}t =
(\exp(r (window - d)) - 1) / r}, which tends to \code{window - d} as \eqn{r} tends
to zero. This is the dynamical bias of Park et al. (2024); for a long window
and a growing epidemic it approaches an exponential tilt of the delay
distribution by \eqn{\exp(-r d)}.
}
\details{
Working on the log scale keeps the weight finite for a fast growing epidemic
observed over a long window, where the weight itself would overflow.
}
\keyword{internal}
