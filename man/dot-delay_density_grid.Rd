% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{.delay_density_grid}
\alias{.delay_density_grid}
\title{Evaluate the density of the delay distribution at each draw}
\usage{
.delay_density_grid(family, dpars, max_delay = NULL, n_grid = 101, nsim = 1000)
}
\arguments{
\item{family}{A delay distribution family as returned by
\code{\link[=.resolve_delay_family]{.resolve_delay_family()}}, a list with the family \code{name} and its
distributional parameters \code{dpars}.}

\item{dpars}{A named list of distributional parameter vectors.}

\item{max_delay}{The largest delay to evaluate the delay distribution at
when \code{type = "delay"}. If \code{NULL}, the default, the posterior median of the
99\% quantile of the delay distribution is used.}

\item{n_grid}{The number of delays to evaluate the density at.}

\item{nsim}{The number of delays to simulate per row of \code{data}. Defaults to
1000. Only used when simulating.}
}
\value{
A list with \code{delays}, the delays the density is evaluated at, and
\code{density}, a matrix with one row per draw and one column per delay.
}
\description{
Uses the density of the family when there is an analytic solution, and
otherwise simulates delays from each draw with \code{.simulate_delays()} and
estimates their density with \code{\link[stats:density]{stats::density()}}.
}
\keyword{internal}
