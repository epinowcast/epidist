% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leave_one_out.R
\name{epidist_leave_one_out}
\alias{epidist_leave_one_out}
\title{Refit a meta model leaving each study out in turn}
\usage{
epidist_leave_one_out(
  fit,
  data = NULL,
  newdata = NULL,
  re_formula = NA,
  width = 0.95,
  keep_fits = FALSE,
  ...
)
}
\arguments{
\item{fit}{A meta model fitted with \code{\link[=epidist]{epidist()}} to an \code{epidist_meta_model}
object.}

\item{data}{The \code{epidist_meta_model} object \code{fit} was fitted to.
Only needed when \code{fit$data} has no \code{study} column, which happens when the
formula does not use \code{study}.
If \code{NULL}, the default, the model data stored in \code{fit} is used.}

\item{newdata}{A \code{data.frame} of data to predict the delay for, passed to
\code{\link[=delay_summary_draws]{delay_summary_draws()}}.
If \code{NULL}, the default, a single row giving the population level delay
with no censoring and no truncation is built with \code{\link[=epidist_newdata]{epidist_newdata()}}.}

\item{re_formula}{Passed to \code{\link[brms:prepare_predictions]{brms::prepare_predictions()}} through
\code{\link[=delay_summary_draws]{delay_summary_draws()}}.
The default \code{NA} switches off any study level term, so the summaries are
of the population level delay.}

\item{width}{The width of the central posterior interval reported for each
summary, and used to flag influential studies.
Defaults to \code{0.95}.}

\item{keep_fits}{If \code{TRUE}, the refits are returned in the \code{fits} attribute
of the result as a list named by the held out study.
Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[brms:update.brmsfit]{brms::update.brmsfit()}} and so
to \code{\link[brms:brm]{brms::brm()}}, such as \code{cores}, \code{chains}, \code{iter}, \code{refresh} and
\code{silent}.}
}
\value{
A \code{tibble} with one row per held out study, row of \code{newdata} and
summary.
The columns are \code{study}, \code{.row}, any predictors of the model present in
\code{newdata}, \code{summary} (\code{"mean"} or \code{"sd"}), \code{estimate}, \code{lower} and \code{upper}
from the held out fit, \code{full_estimate}, \code{full_lower} and \code{full_upper} from
the full fit, \code{shift} and \code{influential}.
The \code{width} attribute records the interval width.
}
\description{
A pooled estimate should not rest on a single study.
This refits a meta model once per study, each time with that study's rows
removed, and compares the natural scale mean and standard deviation of the
delay from each refit with the full fit.
Each refit is exact rather than an importance sampling approximation.
\code{\link[loo:loo]{loo::loo()}} works per observation, which for the meta model is a group of
summaries rather than a study, and the number of studies is usually small
enough that refitting costs little.
}
\details{
The study of each row is the \code{study} column of the model data.
Individual level rows, which \code{\link[=as_epidist_meta_model]{as_epidist_meta_model()}} labels
\code{"individual"}, are held out together as one study.
\code{brms} keeps only the variables the formula uses in a fit, so a model whose
formula does not mention \code{study}, such as \code{mu ~ 1}, has no \code{study} column
in \code{fit$data}.
Pass the \code{epidist_meta_model} object the model was fitted to as \code{data} in
that case.
Removing a study then goes through the \link{epidist_data} methods, so the
object is checked, and through \code{\link[=epidist_transform_data]{epidist_transform_data()}}, so the rows are
summarised as they were for the full fit.

Each refit reuses the compiled model through \code{\link[brms:update.brmsfit]{brms::update.brmsfit()}} with
the data of the held out study removed.
The formula, family, priors and sampler settings of the full fit are kept
unless overridden through \code{...}.
The delay summaries come from \code{\link[=delay_summary_draws]{delay_summary_draws()}} at \code{newdata}.
The default is the population level delay with no censoring and no
truncation, built with \code{\link[=epidist_newdata]{epidist_newdata()}}, with any study level term
switched off by \code{re_formula = NA}.
Every refit must be able to predict \code{newdata}, so it must not name the
held out study, and every level of a factor it uses must remain in the
data once each study is removed.

For each summary the posterior median and the central \code{width} interval of
the held out fit are reported next to those of the full fit.
\code{shift} is the difference between the two medians divided by the posterior
standard deviation of the full fit.
\code{influential} is \code{TRUE} when the held out median lies outside the full
fit's interval, that is when removing the study moves the estimate by more
than the full fit's own uncertainty.
With few studies every refit loses a large share of the evidence, so the
intervals of the refits are wider than the full fit's.
The comparison is of the point estimate against the full fit's interval,
so it is not affected by that.
}
\examples{
\donttest{
estimates <- as_epidist_estimates_data(
  data.frame(
    study = c("A", "A", "B", "B", "C", "C"),
    type = c("mean", "sd", "mean", "sd", "mean", "sd"),
    value = c(7.5, 3.6, 6.4, 3.1, 8.2, 4.0),
    n = c(120, 120, 80, 80, 150, 150),
    relative_obs_time = c(20, 20, 25, 25, Inf, Inf),
    trunc_adjusted = c(FALSE, FALSE, FALSE, FALSE, TRUE, TRUE),
    cens_adjusted = c(0, 0, 0, 0, 1, 1)
  )
)
meta <- as_epidist_meta_model(estimates = estimates)
fit <- epidist(
  meta, chains = 2, cores = 2, refresh = ifelse(interactive(), 250, 0)
)

# The formula does not use study, so pass the model data
epidist_leave_one_out(fit, data = meta, refresh = 0)
}
}
\seealso{
\code{\link[=delay_summary_draws]{delay_summary_draws()}} for the summaries the comparison uses and
\code{\link[=epidist_newdata]{epidist_newdata()}} to build \code{newdata}.

Other meta_model: 
\code{\link{as_epidist_meta_model}()},
\code{\link{as_epidist_meta_model.NULL}()},
\code{\link{as_epidist_meta_model.epidist_aggregate_data}()},
\code{\link{as_epidist_meta_model.epidist_estimates_data}()},
\code{\link{as_epidist_meta_model.epidist_linelist_data}()},
\code{\link{assert_epidist.epidist_meta_model}()},
\code{\link{epidist_family_model.epidist_meta_model}()},
\code{\link{epidist_formula_model.epidist_meta_model}()},
\code{\link{epidist_model_prior.epidist_meta_model}()},
\code{\link{epidist_newdata.epidist_meta_model}()},
\code{\link{epidist_transform_data_model.epidist_meta_model}()},
\code{\link{is_epidist_meta_model}()},
\code{\link{new_epidist_meta_model}()}
}
\concept{meta_model}
