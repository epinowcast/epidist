% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_model.R
\name{as_epidist_meta_model.epidist_linelist_data}
\alias{as_epidist_meta_model.epidist_linelist_data}
\title{The meta model method for \code{epidist_linelist_data} objects}
\usage{
\method{as_epidist_meta_model}{epidist_linelist_data}(
  data,
  estimates = NULL,
  obs_time_threshold = 2,
  weight = NULL,
  delay_min = NULL,
  ...
)
}
\arguments{
\item{data}{An \code{epidist_linelist_data} object.}

\item{estimates}{An \code{epidist_estimates_data} object of published summary
estimates, or \code{NULL}.}

\item{obs_time_threshold}{Ratio used to determine threshold for setting
relative observation times to Inf. Observation times greater than
\code{obs_time_threshold} times the maximum delay will be set to Inf to improve
model efficiency by reducing the number of unique observation times.
Default is 2.}

\item{weight}{A column name containing counts of identical linelist items.
When specified, the user is declaring that rows with the same values
represent the same observation occurring multiple times. This allows for
efficient data representation by storing unique patterns with their counts
rather than repeating identical rows. The marginal model will further
aggregate these counts based on the formula specification. Default is NULL,
which assigns a count of 1 to each row. Internally this is used to define
the 'n' column of the returned object.}

\item{delay_min}{Minimum delay (left truncation point). Can be:
\itemize{
\item \code{NULL} (default): uses a \code{delay_min} column from the data if present,
otherwise defaults to 0 (no left truncation).
\item A numeric scalar: applied to all observations.
\item A column name string: looks up the named column in the data.
This is passed as the \code{L} parameter to
\code{\link[primarycensored:dprimarycensored]{primarycensored::dpcens()}}.
}}

\item{...}{Additional arguments passed to methods.}
}
\description{
Prepares individual level data exactly as
\code{\link[=as_epidist_marginal_model.epidist_linelist_data]{as_epidist_marginal_model.epidist_linelist_data()}} does and then stacks it
with any supplied summary estimates.
}
\examples{
sierra_leone_ebola_data |>
  as_epidist_linelist_data(
    pdate_lwr = "date_of_symptom_onset",
    sdate_lwr = "date_of_sample_tested"
  ) |>
  as_epidist_meta_model()
}
\seealso{
Other meta_model: 
\code{\link{as_epidist_meta_model}()},
\code{\link{as_epidist_meta_model.NULL}()},
\code{\link{as_epidist_meta_model.epidist_aggregate_data}()},
\code{\link{as_epidist_meta_model.epidist_estimates_data}()},
\code{\link{assert_epidist.epidist_meta_model}()},
\code{\link{epidist_family_model.epidist_meta_model}()},
\code{\link{epidist_formula_model.epidist_meta_model}()},
\code{\link{epidist_transform_data_model.epidist_meta_model}()},
\code{\link{is_epidist_meta_model}()},
\code{\link{new_epidist_meta_model}()}
}
\concept{meta_model}
