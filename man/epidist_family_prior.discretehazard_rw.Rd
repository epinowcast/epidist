% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonparametric.R
\name{epidist_family_prior.discretehazard_rw}
\alias{epidist_family_prior.discretehazard_rw}
\alias{epidist_family_prior.discretehazard_re}
\title{Family specific prior distributions for the non-parametric family}
\usage{
\method{epidist_family_prior}{discretehazard_rw}(family, formula, ...)

\method{epidist_family_prior}{discretehazard_re}(family, formula, ...)
}
\arguments{
\item{family}{A description of the response distribution and link function to
be used in the model. Every family function has a link argument allowing
users to specify the link function to be applied on the response variable.
If not specified, default links are used. For details of all supported
families see \code{\link[brms:brmsfamily]{brms::brmsfamily()}}. Commonly used, such as
\code{\link[brms:lognormal]{brms::lognormal()}}, are also
reexported as part of \code{epidist}.}

\item{formula}{An object of class \link[stats:formula]{stats::formula} or \link[brms:brmsformula]{brms::brmsformula}
(or one that can be coerced to those classes). A symbolic description of
the model to be fitted. A formula must be provided for the distributional
parameter \code{mu}, and may optionally be provided for other distributional
parameters.}

\item{...}{Additional arguments passed to \code{fn} method.}
}
\value{
A \code{brmsprior} object.
}
\description{
The intercept of \code{mu}, the logit hazard, gets a normal prior with a
standard deviation of 1.5 centred on \eqn{\mathrm{logit}(1 / K)}, the
hazard of the first of \eqn{K} bins when every bin is equally likely. A
prior centred on a hazard of a half would put half the prior mass on the
first bin, so the prior mean delay would be about a day whatever the bins.
Centred on the bins, the prior median of the mean delay is near the middle
of the bins, with a wide spread. The intercept of \code{hsigma} gets
\code{normal(0, 1)} on the log scale, the prior \code{primarycensored} uses for the
spread of the logit hazards. Each innovation gets \code{std_normal()}, which
makes the offsets non-centred.
}
\seealso{
Other prior:
\code{\link[=epidist_family_prior]{epidist_family_prior()}},
\code{\link[=epidist_family_prior.default]{epidist_family_prior.default()}},
\code{\link[=epidist_family_prior.lognormal]{epidist_family_prior.lognormal()}},
\code{\link[=epidist_model_prior]{epidist_model_prior()}},
\code{\link[=epidist_model_prior.default]{epidist_model_prior.default()}},
\code{\link[=epidist_model_prior.epidist_meta_model]{epidist_model_prior.epidist_meta_model()}},
\code{\link[=epidist_prior]{epidist_prior()}}
}
\concept{prior}
