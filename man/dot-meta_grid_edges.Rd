% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_grid_edges}
\alias{.meta_grid_edges}
\title{The cohort grid distribution function at the two edges of one cell}
\usage{
.meta_grid_edges(
  cell,
  dist,
  args,
  lower = 0,
  cutoff,
  pwindow,
  swindow,
  growth_rate
)
}
\arguments{
\item{cell}{The index of the grid cell, counting from zero.}

\item{dist}{A \code{primarycensored} distribution function name.}

\item{args}{A named list of distribution parameters.}

\item{lower}{The study's minimum delay (its left truncation point).}

\item{cutoff}{The grid cutoff, either the study observation time or
\code{max_delay}.}

\item{pwindow, swindow}{The primary and secondary censoring window widths.}

\item{growth_rate}{The exponential growth rate of primary events.}
}
\value{
The grid distribution function at the lower and upper cell edges,
or two infinities if the grid mass underflows to zero.
}
\description{
A cohort grid is normalised by the mass it holds, so its cumulative sum at
cell \eqn{k} is
\eqn{(F(k \times swindow) - F(L)) / (F(n_{grid} \times swindow) - F(L))}.
A reported quantile therefore needs four distribution function evaluations
rather than the whole grid, and three where the study counted every delay.
This does not hold under an accrual design, which reweights each cell
before renormalising and so must keep the full grid.
}
\keyword{internal}
