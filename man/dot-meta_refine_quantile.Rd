% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_refine_quantile}
\alias{.meta_refine_quantile}
\title{Refine the chord inverse of a continuous implied distribution function}
\usage{
.meta_refine_quantile(chord, p, dist, args, slots, floor, ceiling)
}
\arguments{
\item{chord}{The chord inverse from \code{\link[=.meta_node_quantile]{.meta_node_quantile()}}.}

\item{p}{A probability.}

\item{dist}{A \code{primarycensored} distribution function name, or \code{NULL} to
return the chord inverse alone.}

\item{args}{A named list of distribution parameters.}

\item{slots}{The output of \code{\link[=.meta_row_slots]{.meta_row_slots()}}.}

\item{floor, ceiling}{The delays at the first and last node, which the
refined value is held between.}
}
\value{
A delay.
}
\description{
Where the family quantile function is available, that is for a lognormal
or weibull delay reported by a study that adjusted for censoring and did
not use an accrual design, the implied quantile is
\eqn{Q(F(L) + p (F(D) - F(L)))} exactly. Otherwise Newton steps are taken
from the chord using the implied distribution function and density of
\code{\link[=.meta_implied_prob]{.meta_implied_prob()}} and \code{\link[=.meta_implied_density]{.meta_implied_density()}}, which exist in
closed form for every remaining continuous design with a uniform primary
event. An accrual estimand, or a uniform single interval estimand with a
growing primary event, is defined by linear interpolation between its
nodes, so its chord is left alone, as is a discrete grid.
}
\details{
The Stan mirror \code{meta_family_node_quantile()} cannot evaluate the primary
censored distribution function at a parameter dependent delay, so the
cases refined here are exactly those it can refine with closed forms.
}
\keyword{internal}
