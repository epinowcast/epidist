% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_summaries.R
\name{.meta_grid_box_ll}
\alias{.meta_grid_box_ll}
\title{The joint log likelihood of several quantiles of integer day delays}
\usage{
.meta_grid_box_ll(y, upper, lower, study_n, dist, args, slots)
}
\arguments{
\item{y}{A vector of reported quantile values in non decreasing order.}

\item{upper}{The largest count of delays below each reported day.}

\item{lower}{The smallest count of delays at or below each reported day.}

\item{study_n}{The number of delays the quantiles were computed from.}

\item{dist}{A \code{primarycensored} distribution function name.}

\item{args}{A named list of distribution parameters.}

\item{slots}{The output of \code{\link[=.meta_row_slots]{.meta_row_slots()}}.}
}
\value{
A log probability mass.
}
\description{
Each quantile reported at probability \eqn{p} and landing on day \eqn{y}
says that the empirical distribution function crossed \eqn{p} between the
day below and the day itself, \eqn{N_{\le y - w_s} \le \lceil n p \rceil
- 1} and \eqn{N_{\le y} \ge \lceil n p \rceil}, with \eqn{N_{\le y}} the
number of delays at or below \eqn{y}. The counts at the integer edges
the reported quantiles name form a Markov chain,
\deqn{N_{e_{i+1}} \mid N_{e_i} \sim N_{e_i} + \text{Binomial}\left(n -
N_{e_i}, \frac{G_0(e_{i+1}) - G_0(e_i)}{1 - G_0(e_i)}\right),}
with \eqn{G_0} the uncorrected grid distribution function, and the
likelihood is the probability that every count fell in its box. It is a
forward pass over the counts on the log scale, one step of
\code{\link[=.meta_box_step]{.meta_box_step()}} per edge, kept to a band of \code{\link[=.meta_band_half_width]{.meta_band_half_width()}}
counts around the most likely path of \code{\link[=.meta_box_mode_path]{.meta_box_mode_path()}}, so its
cost grows like the number of edges times the sample size to the power
one and a half rather than squared. Two quantiles reported at the same
value are two constraints at one edge, and reporting the same count at
two values is a box no chain can satisfy, which gives \code{-Inf}.
}
\details{
This is the joint form of \code{\link[=.meta_grid_crossing_ll]{.meta_grid_crossing_ll()}}, which it reduces
to for a single quantile. Like it, the information it carries saturates
as the study grows, and at a thousand delays it is close to an indicator
of the parameters that put the population quantiles in the reported
cells, a box rather than a peak. The multinomial of
\code{\link[=.meta_quantile_set_ll]{.meta_quantile_set_ll()}} on the continuity corrected grid keeps
sharpening with the sample size instead, so it is not used for such a
study.

Matches \code{meta_family_grid_box_ll()} in Stan.
}
\keyword{internal}
