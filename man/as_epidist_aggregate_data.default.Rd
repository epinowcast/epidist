% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_data.R
\name{as_epidist_aggregate_data.default}
\alias{as_epidist_aggregate_data.default}
\title{Create an epidist_aggregate_data object from vectors of event times}
\usage{
\method{as_epidist_aggregate_data}{default}(
  data,
  n = NULL,
  ptime_upr = NULL,
  stime_lwr = NULL,
  stime_upr = NULL,
  obs_time = NULL,
  ...
)
}
\arguments{
\item{data}{Numeric vector giving lower bounds for primary times.}

\item{n}{An integerish vector containing the counts for each row. Must be the
same length as the input data vector.}

\item{ptime_upr}{Numeric vector giving upper bounds for primary times.}

\item{stime_lwr, stime_upr}{Numeric vectors giving lower and upper bounds for
secondary times.}

\item{obs_time}{Numeric vector giving observation times.}

\item{...}{Additional columns to add to the epidist_linelist_data object.}
}
\description{
This method takes vectors of event times (primary/secondary event times and
observation time) along with counts and creates an \code{epidist_aggregate_data}
object. This format is useful when working with pre-aggregated data where
each row represents multiple identical observations with the count stored in
the \code{n} column. Internally it makes use of
\code{\link[=as_epidist_linelist_data.default]{as_epidist_linelist_data.default()}} to convert the data to a linelist
format before adding the count column. See the other methods for other data
input options.
}
\examples{
as_epidist_aggregate_data(
  data = c(1, 2, 3),
  ptime_upr = c(2, 3, 4),
  stime_lwr = c(3, 4, 5),
  stime_upr = c(4, 5, 6),
  obs_time = c(5, 6, 7),
  n = c(1, 2, 3)
)
}
\seealso{
Other aggregate_data: 
\code{\link{as_epidist_aggregate_data}()},
\code{\link{as_epidist_aggregate_data.data.frame}()},
\code{\link{as_epidist_aggregate_data.epidist_linelist_data}()},
\code{\link{assert_epidist.epidist_aggregate_data}()},
\code{\link{is_epidist_aggregate_data}()},
\code{\link{new_epidist_aggregate_data}()}
}
\concept{aggregate_data}
