% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_events}
\alias{plot_events}
\title{Plot the primary and secondary event windows of each case}
\usage{
plot_events(data, obs_time = NULL, by = NULL, n = 200)
}
\arguments{
\item{data}{An \code{epidist_linelist_data} object.}

\item{obs_time}{The observation time to mark with a dashed vertical line,
as a date or a number on the scale of the event times. If \code{NULL}, the
default, no line is drawn.}

\item{by}{A string naming a column of \code{data} to colour the cases by. If
\code{NULL}, the default, the primary and secondary event windows are coloured
differently instead.}

\item{n}{The maximum number of cases to draw. Defaults to 200. Use \code{Inf} to
draw every case.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws one row per case, ordered by primary event time, with the primary and
secondary event windows as horizontal segments joined by a line for the
delay between them. A dashed vertical line marks the observation time when
one is given. This is the plot the vignettes use to show how censoring and
truncation obscure the delays.
}
\details{
Dates are used when \code{data} has the date columns that
\code{\link[=as_epidist_linelist_data.data.frame]{as_epidist_linelist_data.data.frame()}} keeps, \code{pdate_lwr} and so on, and
the numeric time columns otherwise. \code{obs_time} must be on the same scale.

Cases are ordered by the lower bound of their primary event window and
numbered in that order, so the vertical axis shows the growth of the
outbreak. When there are more than \code{n} cases, \code{n} evenly spaced cases in
that order are drawn. This keeps the shape of the outbreak without
over-plotting.

The plot is drawn with \code{\link[ggplot2:ggtheme]{ggplot2::theme_minimal()}} and the colour blind
friendly palette the package documentation uses. Add a theme or a scale of
your own to the returned plot to override either. The column named by \code{by}
is kept in the plot data, so the plot can be faceted by it.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
linelist <- sierra_leone_ebola_data |>
  as_epidist_linelist_data(
    pdate_lwr = "date_of_symptom_onset",
    sdate_lwr = "date_of_sample_tested"
  )
plot_events(linelist, n = 100)
plot_events(linelist, obs_time = as.Date("2014-10-01"), by = "district")
\dontshow{\}) # examplesIf}
}
\seealso{
Other plot:
\code{\link[=plot.epidist_delay_draws]{plot.epidist_delay_draws()}}
}
\concept{plot}
