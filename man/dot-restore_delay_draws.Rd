% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delay_draws.R
\name{.restore_delay_draws}
\alias{.restore_delay_draws}
\title{Put the \code{epidist_delay_draws} class of a template back on an object}
\usage{
.restore_delay_draws(data, template)
}
\arguments{
\item{data}{A \code{data.frame} to put the class back on.}

\item{template}{The object to take the class and its records from.}
}
\value{
\code{data} with the \code{epidist_delay_draws} class, or \code{data} unchanged
when it is not a \code{data.frame} or \code{template} does not have the class.
}
\description{
\code{dplyr} builds a new object rather than keeping the class of its input, so
the class, and the family and stratum variables it records, are lost
whenever the draws are modified. This puts them back, in front of the
classes \code{data} already has, so that a grouped result carries
\code{epidist_delay_draws} followed by \code{grouped_df}. Used by the methods
documented in \link{epidist_delay_draws}.
}
\keyword{internal}
