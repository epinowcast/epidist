% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_epireview.R
\name{.epireview_metadata}
\alias{.epireview_metadata}
\title{Resolve the study metadata of each \code{epireview} record}
\usage{
.epireview_metadata(studies, n, metadata, shared)
}
\arguments{
\item{studies}{A character vector naming the study of each record.}

\item{n}{A numeric vector of the sample size of each record.}

\item{metadata}{A \code{data.frame} of study metadata, or \code{NULL}.}

\item{shared}{A named list of metadata applied to every record.}
}
\value{
A list holding \code{meta}, a \code{tibble} with one row per record and the
\code{study}, \code{n} and metadata columns that were given, and \code{filled}, a list
with one logical vector per column filled marking the records filled.
}
\description{
Starts from the metadata shared by every record, replaces it with the
values \code{metadata} gives for each study, and fills the studies a column
leaves blank with the default \code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}} would assume.
A column no study has a value for is left out, so that
\code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}} assumes it and messages once.
The studies filled are reported by \code{\link[=.epireview_report_gaps]{.epireview_report_gaps()}} once the
records kept are known, so that a dropped record is not named.
}
\keyword{internal}
