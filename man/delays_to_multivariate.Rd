% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_reporting.R
\name{delays_to_multivariate}
\alias{delays_to_multivariate}
\title{Report a set of delays as a published estimate}
\usage{
delays_to_multivariate(
  delays,
  study,
  moments = c("mean", "sd"),
  probs = numeric(0),
  n_bootstrap = 1000,
  ...
)
}
\arguments{
\item{delays}{A numeric vector of observed delays.}

\item{study}{A string naming the study the delays come from.}

\item{moments}{Which moments to report, any of \code{"mean"} and \code{"sd"}.}

\item{probs}{A numeric vector of probabilities to report quantiles at.}

\item{n_bootstrap}{The number of resamples used to estimate the covariance.
Must exceed the number of summaries reported.}

\item{...}{Further columns to attach to every returned row, such as the
sample size \code{n} and the study metadata documented in
\code{\link{as_epidist_estimates_data}()}.}
}
\value{
A list with a \code{data} frame of reported summaries and a \code{vcov} list
holding the covariance matrix, both ready to pass to
\code{\link{as_epidist_estimates_data}()}.
}
\description{
Summarises individual level delays and resamples them to estimate the
covariance between the summaries, giving the format
\code{\link{draws_to_multivariate}()} returns. This is what a site holding a line list it
cannot release would run and publish instead of the line list.
}
\details{
The reported values are the summaries of the delays themselves rather than
the means of the resampled draws, so they are what the study would report.
Only the covariance comes from the resampling.
}
\examples{
set.seed(1)
reported <- delays_to_multivariate(
  rlnorm(200, 1.6, 0.5),
  study = "site A",
  probs = c(0.25, 0.5, 0.75),
  cens_adjusted = 1
)
as_epidist_estimates_data(reported$data, vcov = reported$vcov)
}
\seealso{
Other estimates_data: 
\code{\link{as_epidist_estimates_data}()},
\code{\link{as_epidist_estimates_data.data.frame}()},
\code{\link{assert_epidist.epidist_estimates_data}()},
\code{\link{draws_to_multivariate}()},
\code{\link{is_epidist_estimates_data}()},
\code{\link{new_epidist_estimates_data}()},
\code{\link{parameters_to_multivariate}()}
}
\concept{estimates_data}
