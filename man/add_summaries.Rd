% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{add_summaries}
\alias{add_summaries}
\title{Add natural scale summaries of the delay distribution}
\usage{
add_summaries(
  data,
  family = NULL,
  probs = NULL,
  method = c("auto", "analytic", "sample"),
  nsim = 1000
)
}
\arguments{
\item{data}{A \code{data.frame} of draws of the distributional parameters, as
returned by \code{\link[=delay_parameter_draws]{delay_parameter_draws()}}.}

\item{family}{A model fit with \code{\link[=epidist]{epidist()}}, a \code{brms} family, or
the name of one, giving the delay distribution. If \code{NULL}, the default,
the family is taken from \code{data}, which \code{\link[=delay_parameter_draws]{delay_parameter_draws()}} records
on it. Some \code{dplyr} verbs drop that record, so pass the fit or the family
if \code{data} has been through one of them.}

\item{probs}{A numeric vector of probabilities to add quantiles of the delay
distribution for. If \code{NULL}, the default, no quantiles are added.}

\item{method}{Either \code{"auto"}, the default, which uses the analytic solution
when there is one and simulates otherwise, \code{"analytic"}, which errors when
there is no analytic solution, or \code{"sample"}, which always simulates.}

\item{nsim}{The number of delays to simulate per row of \code{data}. Defaults to
1000. Only used when simulating.}
}
\value{
The input with \code{mean} and \code{sd} columns added, and one column per
element of \code{probs}.
}
\description{
Adds the mean and standard deviation of the delay distribution implied by
each draw of the distributional parameters, and quantiles of that
distribution if \code{probs} is given. Analytic solutions are used for the
families that have one. Every other family is summarised by simulating
delays from it, which works for any family \code{brms} can predict from.
}
\details{
The summaries describe the delay distribution, not the posterior. A row of
\code{data} holds one draw of the distributional parameters, and the columns
added are the mean, standard deviation and quantiles of the delay
distribution those parameters define. Summarise the resulting columns across
draws to get posterior summaries of them.

Quantile columns are named as in \code{\link[posterior:quantile2]{posterior::quantile2()}}, so \code{probs = 0.05}
gives a \code{q5} column.

Simulation adds Monte Carlo error to the summaries. The standard error of
the mean is the delay standard deviation divided by the square root of
\code{nsim}. Simulation is also memory hungry, because it draws \code{nsim} delays for
every row of \code{data}. Reduce the number of rows with \code{\link[=epidist_strata]{epidist_strata()}}, or
the number of draws with the \code{ndraws} argument of
\code{\link[=delay_parameter_draws]{delay_parameter_draws()}}, if it is slow.
}
\examples{
draws <- data.frame(mu = c(1.8, 2.0), sigma = c(0.5, 0.4))
add_summaries(draws, family = "lognormal", probs = c(0.05, 0.95))
}
\seealso{
Other postprocess: 
\code{\link{delay_parameter_draws}()},
\code{\link{epidist_strata}()}
}
\concept{postprocess}
