% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_study.R
\name{simulate_study}
\alias{simulate_study}
\title{Simulate the summaries a published study would have reported}
\usage{
simulate_study(
  data,
  study,
  report = c("moments", "quantiles", "multivariate", "mean_se"),
  probs = c(0.25, 0.5, 0.75),
  cens_adjusted = 0,
  trunc_adjusted = FALSE,
  trunc_design = c("cohort", "accrual"),
  relative_obs_time = Inf,
  delay_min = 0,
  growth_rate = 0,
  n = NULL,
  max_delay = NULL,
  ...
)
}
\arguments{
\item{data}{An \code{epidist_linelist_data} object built from simulated event
times, with the exact \code{ptime} and \code{stime} columns kept by
\code{\link[=simulate_dates]{simulate_dates()}} when \code{keep_times = TRUE}. Every case must use the
same primary window and the same secondary window.}

\item{study}{A string labelling the study.}

\item{report}{What the study published. \code{"moments"} gives a mean and a
standard deviation with the sample size, \code{"quantiles"} the quantiles at
\code{probs} with the sample size, \code{"multivariate"} the mean and standard
deviation with their bootstrap covariance, through
\code{\link[=new_epidist_multivariate]{new_epidist_multivariate()}}, and \code{"mean_se"} a mean with its standard
error and no sample size.}

\item{probs}{The probabilities of the quantiles a \code{"quantiles"} study
reports.}

\item{cens_adjusted}{The censoring adjustment code the study used, one of
0 to 4 as described above and in \code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}}.}

\item{trunc_adjusted}{Whether the study corrected for right truncation. If
\code{FALSE}, the study's truncation is applied to the line list.}

\item{trunc_design}{How the study stopped collecting, \code{"cohort"} or
\code{"accrual"}. Only used when \code{trunc_adjusted} is \code{FALSE}.}

\item{relative_obs_time}{The study's observation time. For a \code{"cohort"}
design this bounds each delay, and for an \code{"accrual"} design it is the
length of the collection window from the start of the line list.
Defaults to \code{Inf}, which is only allowed for a study that adjusted for
right truncation.}

\item{delay_min}{The smallest measured delay the study counted. Cases
below it are dropped.}

\item{growth_rate}{The growth rate the study is described by. Passed
through as metadata and not used to select cases, so it should be the
rate the line list was simulated with.}

\item{n}{The number of cases the study sampled from those it could have
seen. Defaults to \code{NULL}, meaning all of them.}

\item{max_delay}{The grid cutoff passed through to
\code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}}, or \code{NULL} for its default.}

\item{...}{Further columns, such as covariates, added to every row of the
result.}
}
\value{
An \code{epidist_estimates_data} object with one row per reported
summary.
}
\description{
Applies the observation and estimation procedure of one study to a
simulated line list and returns the summaries that study would have
published, with the metadata \code{\link[=as_epidist_estimates_data]{as_epidist_estimates_data()}} needs to
forward model them. The line list must carry the exact event times that
\code{\link[=simulate_dates]{simulate_dates()}} keeps when \code{keep_times = TRUE}, because the censoring
adjustments a study may have used differ in how much of each event time
they saw.
}
\details{
The study measures each delay as its censoring adjustment code says, drops
the cases its truncation would have hidden from it, drops delays below
\code{delay_min}, takes a sample of \code{n} cases and summarises them. Each step
follows the estimand the meta model uses for that code, see
\code{vignette("model")}.

The measured delay is, by \code{cens_adjusted} code:
\itemize{
\item 0, the difference between the lower edges of the secondary and primary
windows, which is the integer date difference of a daily line list.
\item 1, the exact delay between the two events.
\item 2, the exact secondary time less the lower edge of the primary window,
the uniform single interval approximation.
\item 3, the date difference of code 0 plus half a secondary window, midpoint
imputation.
\item 4, the exact secondary time less the midpoint of the primary window.
}

A study that did not adjust for right truncation sees a case only if its
delay had completed by its observation time. Under a \code{"cohort"} design the
delay the observation time bounds is the underlying one, so a study on the
discrete grid (codes 0 and 3) keeps a case only if the whole window its
delay fell in is below \code{relative_obs_time}, and a code 4 study keeps a
case if its code 2 delay is. Under an \code{"accrual"} design the study stopped
at a calendar date \code{relative_obs_time} after the start of the line list,
and keeps every case whose secondary event fell before it.
}
\examples{
linelist <- simulate_gillespie(seed = 1) |>
  simulate_secondary(meanlog = 1.8, sdlog = 0.5) |>
  simulate_dates(keep_times = TRUE) |>
  as_epidist_linelist_data()
simulate_study(
  linelist, "naive snapshot",
  cens_adjusted = 0, trunc_adjusted = FALSE, relative_obs_time = 15
)
}
\seealso{
Other simulate: 
\code{\link{simulate_dates}()},
\code{\link{simulate_exponential_cases}()},
\code{\link{simulate_gillespie}()},
\code{\link{simulate_secondary}()},
\code{\link{simulate_uniform_cases}()}
}
\concept{simulate}
