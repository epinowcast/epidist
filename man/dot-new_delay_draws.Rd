% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{.new_delay_draws}
\alias{.new_delay_draws}
\title{Class constructor for \code{epidist_delay_draws} objects}
\usage{
.new_delay_draws(data, family, vars = NULL)
}
\arguments{
\item{data}{A \code{data.frame} of draws of the distributional parameters, as
returned by \code{\link[=delay_parameter_draws]{delay_parameter_draws()}}.}

\item{family}{A list with the delay distribution \code{name} and its
distributional parameters \code{dpars}, as returned by \code{.delay_family()}.}

\item{vars}{A character vector of the columns of \code{data} that define the
strata, or \code{NULL} when there are none.}
}
\value{
\code{data} with the \code{epidist_delay_draws} class and the
\code{epidist_family} and \code{epidist_vars} attributes.
}
\description{
Records the delay distribution family, and the variables that define the
strata, on a \code{data.frame} of draws of the distributional parameters, and
adds the \code{epidist_delay_draws} class in front of its existing classes. The
class gives the draws a \link[=plot.epidist_delay_draws]{plot()} method. Most
\code{dplyr} verbs rebuild their input and so drop the class and what it
records.
}
\keyword{internal}
