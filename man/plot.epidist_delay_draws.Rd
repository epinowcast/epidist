% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.epidist_delay_draws}
\alias{plot.epidist_delay_draws}
\alias{autoplot.epidist_delay_draws}
\title{Plot posterior draws of the delay distribution}
\usage{
\method{plot}{epidist_delay_draws}(x, ...)

\method{autoplot}{epidist_delay_draws}(
  object,
  type = c("parameters", "delay"),
  by = NULL,
  pars = NULL,
  true_values = NULL,
  ndraws = NULL,
  probs = c(0.05, 0.95),
  max_delay = NULL,
  family = NULL,
  ...
)
}
\arguments{
\item{x, object}{An \code{epidist_delay_draws} object, as returned by
\code{\link[=delay_parameter_draws]{delay_parameter_draws()}}, \code{\link[=delay_summary_draws]{delay_summary_draws()}} or \code{\link[=add_summaries]{add_summaries()}}.}

\item{...}{Passed from \code{plot()} to \code{autoplot()}. Unused otherwise.}

\item{type}{Either \code{"parameters"}, the default, to plot the posterior
density of each parameter, or \code{"delay"} to plot the delay distribution the
draws imply.}

\item{by}{A character vector of columns of \code{object} that define the strata
to colour by. If \code{NULL}, the default, the variables recorded on \code{object}
are used. See the details.}

\item{pars}{A character vector of the columns to plot when
\code{type = "parameters"}. If \code{NULL}, the default, the distributional
parameters of the family are plotted, along with the \code{mean}, \code{sd} and
quantile columns present.}

\item{true_values}{A named numeric vector of true parameter values to mark
with dashed vertical lines when \code{type = "parameters"}. The names must be
among the parameters plotted.}

\item{ndraws}{The number of draws per stratum to plot the delay
distribution of when \code{type = "delay"}, one line each, sampled at random.
If \code{NULL}, the default, the posterior median density is drawn with a
ribbon between the \code{probs} quantiles instead.}

\item{probs}{A numeric vector of two probabilities giving the quantiles the
ribbon spans when \code{type = "delay"}. Defaults to \code{c(0.05, 0.95)}.}

\item{max_delay}{The largest delay to evaluate the delay distribution at
when \code{type = "delay"}. If \code{NULL}, the default, the posterior median of the
99\% quantile of the delay distribution is used.}

\item{family}{A model fit with \code{\link[=epidist]{epidist()}}, a \code{brms} family, or
the name of one, giving the delay distribution. If \code{NULL}, the default,
the family recorded on \code{object} is used.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the draws returned by \code{\link[=delay_parameter_draws]{delay_parameter_draws()}},
\code{\link[=delay_summary_draws]{delay_summary_draws()}} and \code{\link[=add_summaries]{add_summaries()}}. The default,
\code{type = "parameters"}, draws the posterior density of each distributional
parameter, and of any summary column \code{\link[=add_summaries]{add_summaries()}} added, in its own
panel. \code{type = "delay"} draws the delay distribution the draws imply over a
grid of delays, as the posterior median density with a ribbon between two
quantiles, or as one line per draw when \code{ndraws} is given.
}
\details{
The strata of the draws are coloured. By default they are the unique
combinations of the variables in the distributional parameter formulas,
which \code{\link[=delay_parameter_draws]{delay_parameter_draws()}} records, and each \code{.row} when the draws
have several rows of \code{newdata} but no such variables. Pass \code{by} to
stratify by other columns of the draws. The columns in \code{by} are kept in the
plot data, so the plot can be faceted by them.

The delay distribution is evaluated with the density of the family for the
lognormal, gamma and Weibull families. For any other family, delays are
simulated from each draw as in \code{\link[=add_summaries]{add_summaries()}} and their density is
estimated with \code{\link[stats:density]{stats::density()}}. Either way the density is evaluated at
every draw, so thin the draws with the \code{ndraws} argument of
\code{\link[=delay_parameter_draws]{delay_parameter_draws()}}, or build \code{newdata} with \code{\link[=epidist_strata]{epidist_strata()}},
when there are many.

\code{plot()} and \code{autoplot()} are the same function. Both need \code{ggplot2}.

The plot is drawn with \code{\link[ggplot2:ggtheme]{ggplot2::theme_minimal()}} and the colour blind
friendly palette the package documentation uses. Add a theme or a scale of
your own to the returned plot to override either.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
draws <- data.frame(
  mu = rnorm(200, 1.8, 0.05),
  sigma = exp(rnorm(200, log(0.5), 0.05))
) |>
  add_summaries(family = "lognormal", probs = 0.5)
plot(draws, true_values = c(mu = 1.8, sigma = 0.5))
plot(draws, type = "delay")
plot(draws, type = "delay", ndraws = 50)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=delay_parameter_draws]{delay_parameter_draws()}} and \code{\link[=delay_summary_draws]{delay_summary_draws()}} for the
draws, and \code{\link[=plot_events]{plot_events()}} to plot the data.

Other plot:
\code{\link[=plot_events]{plot_events()}}
}
\concept{plot}
