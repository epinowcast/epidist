% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{.draw_chain_iteration}
\alias{.draw_chain_iteration}
\title{Chain and iteration index of each posterior draw}
\usage{
.draw_chain_iteration(object, pp)
}
\arguments{
\item{object}{A model fit with \code{\link[=epidist]{epidist()}}.}

\item{pp}{A \code{brmsprep} object from \code{\link[brms:prepare_predictions]{brms::prepare_predictions()}}.}
}
\value{
A list with \code{chain} and \code{iteration} elements.
}
\description{
\code{brms} stores draws ordered by chain, so the chain and iteration of a draw
follow from its position. A subset of draws cannot be placed this way, so
both are \code{NA} in that case, as they are in \code{tidybayes}.
}
\keyword{internal}
