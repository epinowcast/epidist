% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{.simulate_delays}
\alias{.simulate_delays}
\title{Simulate delays from each draw of the distributional parameters}
\usage{
.simulate_delays(family, dpars, nsim = 1000)
}
\arguments{
\item{family}{A delay distribution family as returned by
\code{\link[=.resolve_delay_family]{.resolve_delay_family()}}, a list with the family \code{name} and its
distributional parameters \code{dpars}.}

\item{dpars}{A named list of distributional parameter vectors.}

\item{nsim}{The number of delays to simulate per row of \code{data}. Defaults to
1000. Only used when simulating.}
}
\value{
A matrix with one row per element of the vectors in \code{dpars} and
\code{nsim} columns.
}
\description{
Simulation goes through the \code{brms} posterior prediction function for the
family, so it works for any family \code{brms} can predict from. Rows are
simulated in chunks to bound the memory used.
}
\keyword{internal}
