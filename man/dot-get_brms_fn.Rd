% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.get_brms_fn}
\alias{.get_brms_fn}
\title{Get the post-processing function of a family by prefix}
\usage{
.get_brms_fn(prefix, family)
}
\arguments{
\item{prefix}{Character string prefix of the brms function to get (e.g.
"log_lik")}

\item{family}{A description of the response distribution and link function to
be used in the model. Every family function has a link argument allowing
users to specify the link function to be applied on the response variable.
If not specified, default links are used. For details of all supported
families see \code{\link[brms:brmsfamily]{brms::brmsfamily()}}. Commonly used, such as
\code{\link[brms:lognormal]{brms::lognormal()}}, are also
reexported as part of \code{epidist}.}
}
\value{
The requested function
}
\description{
Gets the function \code{brms} would call for a family, such as \code{log_lik} or
\code{posterior_predict}. A family \code{epidist} defines itself, such as
\code{\link[=gengamma]{gengamma()}}, carries these functions, so they are taken from it, whether
it is given as the family or as a model family built on it. For a \code{brms}
family they come from \code{\link[=.brms_family_fn]{.brms_family_fn()}}.
}
\keyword{internal}
