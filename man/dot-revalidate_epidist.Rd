% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epidist_data.R
\name{.revalidate_epidist}
\alias{.revalidate_epidist}
\title{Re-check a modified \code{epidist} object and drop any classes it fails}
\usage{
.revalidate_epidist(data, original = NULL)
}
\arguments{
\item{data}{A modified \code{epidist} object. An object with no columns is never
valid, and is unclassed without a warning because it is usually the
prototype \code{vctrs} takes rather than something the user asked for.}

\item{original}{The object before it was modified. Checking is skipped when
the modification left the object unchanged.}
}
\value{
\code{data} with any \code{epidist} class whose requirements it no longer
meets removed.
}
\description{
Checks \code{data} against each of its \code{epidist} classes using
\code{\link[=.check_epidist_class]{.check_epidist_class()}} and drops those it no longer meets the
requirements of. Modifications that leave the object unchanged are not
checked. Used by the methods documented in \link{epidist_data}.
}
\keyword{internal}
